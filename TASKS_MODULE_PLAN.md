# מודול "ניהול משימות" - תוכנית שלבים ופרומפטים

מקור: `אפיון_מקצועי_משימות_ותזכורות_עולם_התורה_V2.docx` (גרסה 2.0, לאחר הערות לקוח).
החלטה: בנייה ישירה בקוד הקיים (Claude Code) - **לא** Lovable. אזור רביעי במערכת, לצד תפעול/כספים/ניהול.

איך להשתמש בקובץ הזה: כל שלב הוא פרומפט עצמאי. מעתיקים את הטקסט שמתחת ל"פרומפט:" ומדביקים בשיחה עם Claude Code כשמוכנים להתקדם - בדיוק כמו בכל שאר השלבים של הפרויקט. אין להתקדם לשלב הבא לפני שהקודם נבדק ואושר.

## החלטות עסקיות שעדיין פתוחות (מהאפיון, סעיף 22)

אלה לא עניין טכני - הן דורשות תשובה שלך/של הלקוח לפני **שלב 21 (WhatsApp)** בלבד. שאר השלבים לא תלויים בהן:

1. אילו קבוצות WhatsApp יחוברו בשלב הראשון.
2. באיזה Integration/Provider ייעשה החיבור (למשל Twilio WhatsApp Business API / Meta Cloud API / 360dialog) - דורש פתיחת חשבון אצל ספק חיצוני, לא רק קוד.
3. האם כל הודעה נכנסת לתיבת סיווג, או רק הודעות עם מילת פתיחה/תגובה לבוט.
4. האם מותרת המרה אוטומטית לפי פורמט הודעה מובנה.
5. האם סטטוס שהשתנה במערכת יישלח בעתיד חזרה ל-WhatsApp (ברירת מחדל: לא, בשלב ראשון).
6. האם משימה שהושלמה דורשת הערת סיום בקטגוריות מסוימות.

---

## שלב 18 - ליבה: מסד נתונים, הרשאות ו-RLS

**פרומפט:**
> בוא נתקדם לשלב 18: מודול ניהול משימות - מסד נתונים ליבה והרשאות, לפי `TASKS_MODULE_PLAN.md`.

**מה נבנה:**
- טבלאות: `teams`, `team_members` (קונסטרוקט חדש לגמרי - לא קיים היום במערכת), `task_categories`, `tasks`, `task_owners` (many-to-many), `task_teams` (many-to-many), `task_watchers`, `task_checklist_items`, `task_comments`, `task_links` (קישור פולימורפי לישויות קיימות: עמותה/סניף/קבוצה/תלמיד/מסמך/אצוות יבוא/שגיאת תלמוד/חודש כספי/תרומה/חלוקה/מס"ב/תנועת בנק/ביקורת - לפי סעיף 17 באפיון), `task_history` (יומן שינויים מינימלי לפי סעיף 20 - לא audit_events הכללי, כי צריך שדות ייעודיים כמו "הוסף אחראי X").
- **חשוב: אף טבלה במודול הזה לא מקבלת עמודות `is_demo`/`demo_batch_id`** - לפי דרישת האפיון (סעיף 10, 25), ובניגוד לכל שאר הטבלאות העסקיות במערכת. לא לגעת ב-`delete_demo_batch()`/`preview_demo_batch_deletion()` הקיימים - הטבלאות האלה לא נכנסות לרשימה שלהם, וזה נכון ומכוון.
- הרשאות חדשות בטבלת `permissions`: `area_tasks/access` + 13 ההרשאות מסעיף 11 (`tasks.access`, `tasks.view_assigned`, `tasks.view_team`, `tasks.view_all`, `tasks.create`, `tasks.edit`, `tasks.assign_users`, `tasks.assign_teams`, `tasks.complete`, `tasks.reopen`, `tasks.manage_templates`, `tasks.manage_settings`, `tasks.manage_whatsapp`, `tasks.convert_whatsapp`). **ללא הענקה אוטומטית לפי תפקיד קבוע בקוד** (חוץ מ-system_admin שמקבל הכל, כמו בכל שאר ההרשאות) - הקצאה ראשונית תיעשה במסך הניהול (שלב 19), בדיוק כפי שהאפיון מבקש במפורש.
- `status` עם check constraint: `draft/open/in_progress/waiting/blocked/completed/cancelled/archived`. איחור מחושב (לא סטטוס ידני). `priority`: `low/normal/high/urgent`.
- RLS על `tasks` - זו נקודת ה-RLS הכי מורכבת שנבנתה עד היום במערכת: `has_permission('tasks','view_all')` **או** (`has_permission('tasks','view_team')` וקיימת שורה ב-`task_teams` לצוות שהמשתמש חבר בו) **או** (`has_permission('tasks','view_assigned')` והמשתמש הוא owner/watcher של המשימה). לפעולות כתיבה - `tasks.create`/`tasks.edit`/`tasks.assign_users`/`tasks.assign_teams`/`tasks.complete`/`tasks.reopen` בהתאמה, באותו דפוס כמו `has_permission()` בכל שאר המערכת.
- קישור לישות קיימת (`task_links`) **לא** מעניק הרשאה לראות מידע רגיש בישות המקור - נאכף כי ה-RLS של הישות המקורית (למשל `students`, `donations_view`) נשאר בתוקף בפני עצמו, ה-`task_links` רק שומר מזהה+סוג.

**אימות:** typecheck/build, בדיקת RLS אדוורסרית (משתמש עם `view_assigned` בלבד לא רואה משימה של אחר), בדיקה שהמיגרציה של ניקוי נתוני דמו הקיימת (064) לא צריכה עדכון.

---

## שלב 19 - אזור ניווט ומסכים בסיסיים

**פרומפט:**
> בוא נתקדם לשלב 19: מודול ניהול משימות - אזור ניווט ומסכים בסיסיים, לפי `TASKS_MODULE_PLAN.md`.

**מה נבנה:**
- הוספת `"tasks"` ל-`Area` union ב-`AreaContext.tsx` + `AREA_PERMISSION` (`area_tasks/access`) + מיפוי ב-`Layout.tsx` (`AREA_LABEL`/`AREA_ICON`/`AREA_TEXT`/`AREA_BG_SOFT`/`AREA_TAB_ACTIVE`). שם התצוגה לאזור ייבחר בשלב הזה (לפי סעיף 6.1 - לא חוסם: "משימות ותזכורות"/"ניהול משימות"/"המשימות שלנו").
- מסכים חדשים ב-`screens.ts`: `/tasks` (בית אישי), `/tasks/all`, `/tasks/team`.
- **מסך בית אישי**: משימות להיום, קרובות, באיחור, ממתינות/חסומות, מונה הודעות WhatsApp ממתינות לסיווג, כפתור יצירת משימה. ללא ציון/תפוקה/דירוג בשום מקום במסך.
- **מסך כל המשימות**: חיפוש טקסט, סינון (אחראים/צוותים/סטטוס/קטגוריה/עדיפות/תאריך), מיון, `DataTable` הקיים (עם `columnPicker`+`pagination`, אותה טבלה שכל שאר המסכים במערכת משתמשים בה).
- **כרטיס משימה** (יצירה/עריכה): כותרת, תיאור, סטטוס, עדיפות, קטגוריה, כמה אחראים, כמה צוותים, צופים, יעד+שעה אופציונלית, Checklist, קישור לישות, הערות, קבצים, פעולות (התחלה/המתנה/חסימה/השלמה/ביטול/פתיחה מחדש) לפי הרשאה.
- **מסך צוותים**: בחירת צוות אחד/כמה, צפייה במשימות מורשות.
- ניהול צוותים/הרשאות עצמו (`/tasks/settings`) - במסגרת השלב הזה או כהמשך ישיר, לפי היקף בפועל.

**אימות:** typecheck/build, בדיקה חיה - יצירת משימה עם כמה אחראים וכמה צוותים, ווידוא שמשתמש עם `view_assigned` בלבד לא רואה אותה עד שהוא אחראי/צופה.

---

## שלב 20 - תזכורות ומשימות חוזרות

**פרומפט:**
> בוא נתקדם לשלב 20: מודול ניהול משימות - תזכורות ומשימות חוזרות, לפי `TASKS_MODULE_PLAN.md`.

**מה נבנה:**
- טבלת `task_reminders` (תאריך מוגדר או יחסי ליעד, שעה אופציונלית, לכל האחראים או למשתמשים ספציפיים, Snooze).
- מנוע תזכורות **שרתי** (לא תלוי בדפדפן פתוח - per סעיף 13) - צריך מנגנון תזמון בצד השרת (למשל Supabase Edge Function + `pg_cron`, שאינו קיים עדיין בפרויקט - תשתית חדשה). ערוץ הבסיס: תזכורת בתוך המערכת.
- טבלת `task_recurrence_rules` (תדירות יומית/שבועית/חודשית/שנתית/מותאם, תאריך התחלה/סיום, יום בשבוע/בחודש, אחראים/צוותים/קטגוריה/עדיפות/תזכורות, מדיניות "מופע חדש גם אם הקודם לא הושלם").
- פונקציית generator ליצירת המופע הבא + **מניעת כפילות** (לא ליצור מופע פעמיים לאותו תאריך).

**אימות:** typecheck/build, בדיקה שהתזכורת מופעלת גם בלי שהדפדפן פתוח (בדיקה בצד השרת, לא רק ב-UI), בדיקה שכלל חוזר לא יוצר כפילות בהרצה חוזרת.

---

## שלב 21 - קליטת משימות מ-WhatsApp

**דורש קודם תשובה על ההחלטות העסקיות בראש הקובץ (סעיפים 1-2 לפחות - איזה ספק וכמה קבוצות).**

**פרומפט:**
> בוא נתקדם לשלב 21: מודול ניהול משימות - קליטת WhatsApp, לפי `TASKS_MODULE_PLAN.md`. הספק שנבחר: [להשלים].

**מה נבנה:**
- טבלאות: `whatsapp_groups` (קבוצות מאושרות, שיוך לצוות אחד/כמה), `whatsapp_messages` (טקסט, שולח, מועד, קבוצה, קובץ/תמונה, ציטוט אם נתמך, **מזהה הודעת מקור ייחודי למניעת כפילות**), קישור `whatsapp_messages.converted_task_id`.
- Webhook endpoint (Edge Function) לספק שנבחר - קליטה, dedup לפי מזהה הודעה, שמירה.
- מסך `/tasks/whatsapp`: הודעות חדשות/הומרו/סומנו כלא-משימה, סינון לפי קבוצה/שולח, Preview לקבצים לפי הרשאה, פעולת "הפוך למשימה" (עריכת כותרת/תיאור, בחירת אחראים/צוותים, יעד/תזכורת, קישור לישות, או סימון כ"לא משימה").
- הרשאות `tasks.manage_whatsapp` (חיבור/ניתוק קבוצות) ו-`tasks.convert_whatsapp` (המרה למשימה) נאכפות.
- **גבולות מפורשים לשלב זה**: אין פרסום אוטומטי חזרה לקבוצה, אין המרה של כל הודעה ללא סינון/אישור, אין מידע רגיש מהמערכת מתפרסם בקבוצה.

**אימות:** בדיקה חיה מול הקבוצה האמיתית - הודעה כפולה (retry מהספק) לא יוצרת רשומה כפולה; המרה למשימה שומרת קישור למקור.

---

## שלב 22 - תבניות, Kanban/לוח שנה, חיפוש, QA סופי

**פרומפט:**
> בוא נתקדם לשלב 22: מודול ניהול משימות - תבניות, לוחות, חיפוש ו-QA סופי, לפי `TASKS_MODULE_PLAN.md`.

**מה נבנה:**
- `task_templates` + `task_template_checklist_items` (כותרת/תיאור/קטגוריה/עדיפות/אחראים/צוותים/יעד יחסי/Checklist/תזכורות/משימות משנה), הרשאת `tasks.manage_templates`.
- אוטומציות: כלל (אירוע/לוח זמנים + תנאים + תבנית + אחראים/צוותים + מועד יחסי + מניעת כפילות + הפעלה/השהיה/כיבוי מהניהול).
- `/tasks/board` (Kanban לפי סטטוס, Drag & Drop רק עם הרשאת עריכה), `/tasks/calendar` (יום/שבוע/חודש).
- שילוב ב-`GlobalSearch` הקיים - משימות נכנסות לחיפוש הגלובלי כמו כל ישות אחרת.
- **סריקת QA כמו שלב 16 בפרויקט המקורי**: אבטחה (RLS אדוורסרי - במיוחד ה-view_assigned/view_team/view_all), ביצועים (אינדקסים על `tasks`/`task_owners`/`task_teams`, לא N+1 ברשימות), נגישות (RTL, מקלדת, labels), Loading/Empty/Error/Partial states בכל מסך.

**אימות:** typecheck/build + בדיקה חיה מקצה לקצה של המודול כולו: יצירת משימה ידנית → הקצאת כמה אחראים וצוותים → תזכורת → הודעת WhatsApp שמומרת למשימה → תבנית → Kanban → חיפוש גלובלי.

---

---

## סטטוס: שלבים 18-22 הושלמו (2026-07-26)

כל 5 השלבים בתוכנית זו נבנו, עברו typecheck/build נקי, וביקורת אבטחה ייעודית (כמו
שלב 16 בפרויקט המקורי - מצאה 4 ממצאים אמיתיים, כולם תוקנו ב-071). הבדל אחד מהתוכנית
המקורית: "אוטומציות" (סעיף 16 באפיון, אירוע-מקור גנרי) נדחו במכוון - לוח-זמנים כבר
מכוסה ע"י משימות חוזרות (שלב 20), ו"אירוע מקור" שרירותי דורש תשתית רוחבית שלא הוגדרה
מספיק באפיון כדי לבנות בלי להמציא כללים. שלב 21 (WhatsApp) בנוי ופועל, אך עדיין לא
מחובר לספק אמיתי - זו החלטה עסקית פתוחה (איזה ספק) שתידרש לפני שהודעות אמיתיות יתחילו
להיקלט.

## סיכום קריטריוני קבלה (מהאפיון, סעיף 23)

לפני שהמודול נחשב גמור: האזור מופיע רק למורשה; הרשאות ניתנות לשינוי מהניהול; אין שדות שעות/מדדי תפוקה/דוחות השוואת עובדים בשום מקום; הערות ניתנות לעריכה ללא הגבלת זמן; הודעת WhatsApp כפולה לא נקלטת פעמיים; משימה חוזרת לא נוצרת בכפילות; קישור לישות לא עוקף את ההרשאות של הישות המקורית; אין seed/נתוני דמו במודול.
