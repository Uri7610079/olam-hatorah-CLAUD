# הרצת Migrations

אין Supabase CLI/Docker זמינים בסביבת הפיתוח הנוכחית, לכן migrations מורצים **ידנית**:

1. פתחו את פרויקט ה-Supabase שלכם → **SQL Editor**.
2. פתחו את קובץ ה-migration הבא לפי הסדר המספרי (`001_...`, `002_...` וכו') — **לעולם לא לדלג ולא להריץ מחוץ לסדר**.
3. הדביקו את התוכן, הריצו (Run), ודאו שאין שגיאה.
4. אחרי הרצה מוצלחת — לא לערוך את אותו קובץ שוב. תיקון נעשה בקובץ migration חדש וממוספר.

כל קובץ migration כולל בתחילתו הערה עם תקציר של מה הוא עושה ומאיזה שלב הוא (Task List).

## חריגה מתועדת: 008/009 נערכו בדיעבד, למרות שכבר רצו

ב-20-22.07.2026 בוצעה ביקורת שמצאה 2 בעיות בקוד של שלב 4 (RLS שמאפשר עקיפת מכונת
המצבים של תלמיד; חשבון בנק שלא נסגר כשמוסיפים חדש). באותו זמן 008/009 עדיין לא היו
committed ב-git, ולכן נערכו במקום ישירות בדיסק - אבל התברר בדיעבד שהם **כבר רצו** נגד
הפרויקט החי לפני העריכה. המשמעות:

- **הקבצים 008/009 על הדיסק כרגע** מתארים את הגרסה המתוקנת - נכונים להתקנה טרייה
  עתידית (פרויקט Supabase חדש שמריץ הכול מ-001 ברצף).
- **הפרויקט הקיים** עדיין רץ על הגרסה הישנה (הבאגית) של 008/009, ולכן קיבל **migration
  נפרד, `011_patch_live_stage4_fixes.sql`**, שמבצע ALTER על מה שכבר קיים (drop policy,
  create or replace function) במקום CREATE מאפס - בלי לגעת בנתונים.
- 011 בטוח להרצה גם על התקנה טרייה שכבר קיבלה את 008/009 המתוקנים (כל הפעולות
  idempotent-safe) - אז אין נזק אם מישהו ירוץ אותו על שני הסוגים בטעות.

**מסקנה לעתיד**: לפני שעורכים migration קיים "כי הוא עוד לא committed", לוודא קודם
מול מי שמריץ אותו אם הוא כבר רץ נגד הפרויקט החי בפועל - מעמד ב-git ומעמד ב-DB הם שתי
עובדות נפרדות.

## חריגה דומה: 013/015 (שער סגירת אצווה יבוא)

באותו אופן, אחרי שהמשתמש כבר הריץ את 013 (הגרסה המקורית של commit_import_batch, שחסמה
סגירת אצווה גם על שורות invalid ולא רק needs_decision - מה שהיה תוקע אצווה עם שורה
ריקה לצמיתות בלי דרך לסגור אותה), 013 עודכן בדיסק לגרסה המתוקנת (נכון להתקנה טרייה),
ו-`015_fix_import_commit_gate.sql` הוא ה-patch (create or replace, idempotent) לפרויקט
הקיים.

## שלב 6: 016 הוא patch, לא stage חדש

`016_patch_live_student_status_values.sql` מרחיב את `students.status` (שכבר רץ ב-008)
כדי לכלול `sent_to_talmud`/`active_with_error`, לפי מכונת המצבים המלאה באפיון V3 §10 -
פער שהתגלה תוך כדי בניית שלב 6 (יצוא/יבוא זכאות/שגויים תלוי בסטטוסים האלה). 008 עודכן
בדיסק לרשימה המלאה עבור התקנה טרייה עתידית; 016 הוא ה-ALTER לפרויקט הקיים. 017 (יצוא)
חדש לגמרי, אין בו חריגה.

## 020: עוד patch שהתגלה בבדיקה חיה (לא בביקורת עצמית)

018/019 עברו typecheck/build ואת הביקורת העצמית, אבל בבדיקה בפועל בדפדפן התגלה: הטריגר
`import_batches_enforce_commit` (013) חוסם כל נסיון לסמן `status='committed'` בלי לוודא
קודם שהדגל `app.allow_batch_commit` מופעל - וזה בדיוק מה ש-`commit_eligibility_batch()`/
`commit_errors_batch()` ניסו לעשות בלי להפעיל אותו. 018/019 תוקנו על הדיסק;
`020_patch_live_talmud_commit_gate.sql` הוא ה-patch (create or replace, idempotent)
לפרויקט הקיים. שיעור: טריגר guard שנוצר בשלב אחד (013) צריך להיבדק מול **כל** RPC עתידי
שכותב לאותה טבלה, לא רק ה-RPC שבשבילו הוא נוצר במקור.

## 021: תיקון תצוגה שהתגלה בבדיקה חיה - ספירות לא מתעדכנות אחרי commit

לאחר ש-020 תיקן את חסימת ה-commit, נמצא שההיסטוריה עדיין מציגה ספירות "תקין/דורש החלטה/
שגוי" מרגע יצירת האצווה - commit_eligibility_batch()/commit_errors_batch() לא סנכרנו את
import_batches.valid_count/needs_decision_count/invalid_count אחרי שהלולאה שינתה סטטוס
שורות (למשל תלמיד שלא הותאם). לא באג נתונים (monthly_eligibility/talmud_errors עצמם
נכונים), רק תצוגה. תוקן ב-018/019 על הדיסק; `021_patch_live_talmud_count_sync.sql` הוא
ה-patch לפרויקט הקיים, כולל תיקון רטרואקטיבי לאצוות שכבר נסגרו.

## תיקון נוסף (לא-DB): מסכי "זכאות" ו"מרכז שגיאות" קיבלו את אותה יכולת "חזרה מההיסטוריה"
שכבר הייתה במרכז היבוא הכללי (שלב 5) - עד עכשיו לא היה אפשר לחזור לאצווה אחרי ניווט/רענון
לפני commit. אין migration בשביל זה - השינוי כולו בצד הלקוח.

## 027: פער RLS ישן (שלבים 3/5) שהתגלה בביקורת עצמית לקראת שלב 8, לא בבדיקה חיה

`organizations_select`/`branches_select`/`groups_select` (005/006, שלב 3) ו-
`study_codes_select` (012, שלב 5) התירו SELECT רק למי שיש לו `area_ops` - לא
`area_finance`. הפער לא התגלה בבדיקות החיות של שלבים 3-7 כי כל הבדיקה עד כה בוצעה עם
המשתמש של צ'ני, שהיא `system_admin` (כל ההרשאות דרך ה-cross join ב-001) - משתמש
`finance_controller`/`accountant_readonly` טהור לא היה יכול לקרוא אפילו את שם העמותה,
מה שהיה שובר בשקט את כל מסכי הכספים (כולל רטרו/מכסות משלב 7 וכל שלושת המסכים החדשים
בשלב 8, שכולם טוענים רשימת עמותות/סניפים/קבוצות ישירות; מסך כללי העמלה טוען גם קודי
לימוד). תוקן ב-006/012 על הדיסק (נכון להתקנה טרייה); `027_patch_live_finance_read_access.sql`
הוא ה-patch (drop policy if exists + create, idempotent) לפרויקט הקיים. שם/קוד עמותה-
סניף-קבוצה וקודי לימוד אינם נתון רגיש - ההגנה על מספר חשבון בנק קיימת בנפרד (view
ממסך + reveal RPC), ולא מושפעת משינוי הזה.

**החלטה פתוחה שנשארה מכוונת (לא תוקנה)**: `students_select`/`student_assignments_select`
(009, שלב 4) נשארו `area_ops` בלבד - לא הורחבו ל-area_finance. המשמעות: בכלל עמלה עם
"חריג לתלמיד" ספציפי (מסך כללי העמלה, שלב 8), משתמש `finance_controller` טהור (בלי
area_ops) יראה רשימת תלמידים ריקה לבחירה - שאר המסך (כללים ברמת עמותה/קבוצה/קוד לימוד,
סימולציה) עובד לו כרגיל. זו הכרעת פרטיות מכוונת, לא באג: שם/קוד עמותה-סניף-קבוצה הם
מבנה ארגוני גנרי, אבל רשימת שמות תלמידים היא כבר PII תפעולי - הרחבה שלו לכל תפקיד כספים
היא החלטת scope שצריכה אישור מפורש, לא ברירת מחדל שקטה. בפועל system_admin/
operations_manager (שיש להם גם commission_rules.manage וגם area_ops) יכולים ליצור כלל
חריג לתלמיד ללא מגבלה.

**מסקנה לעתיד**: בדיקה חיה שמבוצעת תמיד עם משתמש system_admin עלולה להחמיץ פערי RLS
שמשפיעים רק על תפקידים מצומצמים יותר - כדאי, בשלב מוקדם, לבדוק לפחות פעם אחת עם משתמש
בעל הרשאות מצומצמות (למשל finance_controller בלבד) כדי לתפוס פערים כאלה ישירות.

## 028: הרחבת גישת כספים לתלמידים - החלטה מפורשת של צ'ני (לא באג), 2026-07-23

ה"החלטה הפתוחה" שתועדה מיד אחרי שלב 8 (`students_select`/`student_assignments_select`/
`student_bank_accounts_select` נשארים area_ops בלבד) הוכרעה במפורש על ידי צ'ני: "נעשה
שכספים יהיה פתוח לתלמידים והפוך". נבדק גם הכיוון ההפוך (תפעול פתוח לכל מה שכספים
רואה) - לא נמצאה אף מדיניות select שמתירה area_finance בלי להתיר גם area_ops, כך
שהכיוון ההפוך כבר היה תקין מאליו בכל המיגרציות עד כה; לא נדרש שינוי בשבילו.
תוקן ב-009 על הדיסק (נכון להתקנה טרייה); `028_patch_live_finance_students_access.sql`
הוא ה-patch לפרויקט הקיים. מספר חשבון הבנק עצמו נשאר ממוסך כרגיל (view + reveal RPC,
מוגן בנפרד בהרשאת `bank_accounts.view_sensitive` שכבר יש ל-finance_controller) - זו
הרחבת גישה לקיום השורה, לא לנתון הרגיש.

## 034: ביקורת רוחבית לקראת שלב 11 - אותה מחלקת באג guard-trigger, שלוש פעמים נוספות

לאחר שלב 10 בוצעה ביקורת רוחבית (סוכן ייעודי) על כל 33 המיגרציות, כדי לתפוס מראש דפוסי
באג שכבר נמצאו-ותוקנו פעם אחת (donations/distribution_batches, שלב 9): טריגר guard
שבודק רק `before update`, כך ש-INSERT ישיר (שדורש רק הרשאה, לא ערך status תקין) יכול
היה ליצור שורה שכבר במצב "מאושר"/סופי מרגע הלידה - עוקף לגמרי את כל הבדיקות שה-RPC
הייעודי אמור לאכוף.

נמצאו שלוש הישנויות נוספות של אותה מחלקת באג בדיוק:

- **`students` (008, שלב 4) - החמורה מכולן.** `enforce_student_status_transition` בדקה
  רק UPDATE. INSERT ישיר יכול היה ליצור תלמיד שכבר "active"/"sent_to_talmud" מרגע
  הלידה, עוקף את `advance_student_status()` (טלפון תקין, שיוך פעיל, חשבון מאומת) -
  ומזין ישירות למחזור זכאות/עמלה/חלוקה/מס"ב שכבר נבנה בשלבים 6-10. זו ההישנות
  החמורה ביותר כי יש לה נתיב ישיר לכסף אמיתי.
- **`import_batches` (013, שלב 5).** `enforce_import_batch_commit` בדקה רק UPDATE.
  INSERT ישיר יכול היה ליצור אצווה שכבר "committed" בלי לעבור את הבדיקה ב-
  `commit_import_batch()` (אין שורות needs_decision פתוחות). חומרה נמוכה יותר - אצווה
  כזו לא כותבת בפועל לשום טבלת יעד, רק "נראית" סגורה.
- **`groups` (010, שלב 3-fix).** `enforce_group_leader_transition` בדקה רק UPDATE.
  קבוצה יכלה להיווצר עם `group_leader_id` קבוע ישירות ב-INSERT, בלי שורת
  `group_leader_assignments` תואמת - פער בהיסטוריה, לא סיכון כספי (הלקוח הקיים כבר
  אף פעם לא עושה את זה - יוצר קבוצה בלי ראש ואז קורא ל-`reassign_group_leader()`).

תוקן ב-008/010/013 על הדיסק (וב-011, שמגדירה מחדש את אותו טריגר students ורצה אחרי
008 בהתקנה טרייה - אחרת הייתה דורסת את התיקון); `034_patch_live_audit_insert_guards.sql`
הוא ה-patch לפרויקט הקיים.

ביקורת נוספת שאותו סוכן ביצע: `organization_officeholders_select` (006) הייתה
`area_ops` בלבד, למרות שבעלי תפקידים/מורשי חתימה רלוונטיים גם לאישור תשלומים בצד
הכספים (אותו דפוס בדיוק כמו organizations/branches/groups שכבר הורחבו ב-027) - הורחבה
גם היא (006 על הדיסק + 034 לפרויקט הקיים).

**נמצא אך לא תוקן (cleanup עתידי, לא באג פעיל)**: הרשאות `financial_months.open_close`
ו-`exports.perform` (שתיהן מ-001) מוענקות ל-finance_controller אך אינן נבדקות בשום
policy/RPC בפועל - שיירים מתכנון מוקדם שהוחלף בהרשאות אחרות (`financial_periods.manage`
בשלב 8, `talmud.export`/`masav.*` בשלבים 6/10). וגם: `export_batches`/
`export_batch_students` (017) חסרות טריגר audit גנרי כמו שאר הטבלאות העסקיות - יצירת
האצווה כן מתועדת ידנית בתוך `create_talmud_export()`, אז זה פער קטן, לא שקט מוחלט.
שני אלה מתועדים כאן לטיפול בשלב 15 (ניקוי לפני מסירה), לא דחוף.

**מסקנה לעתיד**: הבאג הזה חוזר כי כל טבלה חדשה עם מכונת מצבים מקבלת guard trigger
משלה, ומי שכותב אותו נוטה "לחשוב UPDATE" (זה השינוי הכי אינטואיטיבי לחסום) ולשכוח
ש-RLS `for insert` בדרך כלל בודקת רק הרשאה, לא ערך שדה. **מכאן ואילך: כל guard trigger
חדש על טבלה עם מכונת מצבים חייב לכלול `tg_op = 'INSERT'` מפורש מהכתיבה הראשונה**, לא
להתגלות בביקורת מאוחרת.

## 037: suggest_transaction_types() (036, שלב 11) נכשלה בבדיקה חיה הראשונה שלה

`UPDATE bank_transactions bt SET ... FROM LATERAL match_recognition_rule(bt.col, ...) r`
נכשלה בפועל עם `invalid reference to FROM-clause entry for table "bt"`. הסיבה:
PostgreSQL לא מאפשר ל-LATERAL בתוך FROM של UPDATE להתייחס לטבלת היעד של ה-UPDATE עצמה
(bt) - רק לפריטי FROM אחרים. לא נתפס בביקורת עצמית כי זו לא טעות תחבירית (הפונקציה
עברה typecheck/הרצת migration בלי שגיאה - השגיאה מופיעה רק כשה-RPC בפועל נקרא). נתפס
מיד בבדיקה חיה הראשונה של הכפתור "הרצת הצעות סיווג". תוקן ע"י self-join דרך alias שני
(bt2, פריט FROM לגיטימי) + WHERE שמקשר bt ל-bt2 לפי id. תוקן ב-036 על הדיסק;
`037_patch_live_suggest_transaction_types.sql` הוא ה-patch לפרויקט הקיים.

**מסקנה לעתיד**: `UPDATE target ... FROM LATERAL func(target.col)` הוא דפוס שנראה
תקין אך לא קיים ב-PostgreSQL - הביטוי הנכון תמיד דורש alias שני של אותה טבלה
(self-join) כדי ש-LATERAL תוכל להתייחס אליו. שווה לבדוק מראש בפעם הבאה שנעשה שימוש
בדפוס "UPDATE עם LATERAL correlated לטבלת היעד" (למשל בשלב 12, בהתאמות בנק).

## שלב 12: 038-041 - התאמות בנק, חריגות, החזרות וסגירת חודש

`038_stage12_bank_matches.sql`: טבלת `bank_matches` (יעד גנרי `target_table`/`target_id` -
לא עמודת FK נפרדת לכל סוג יעד), ללא INSERT/UPDATE policy ללקוח (RPC-only, כמו
`masav_batches`/`bank_transactions`). `suggest_masav_matches()` מציעה התאמה set-based בין
תנועות חובה לאצוות מס"ב ששודרו (יעד ברמת **האצווה**, לא השורה הבודדת - תנועת בנק אחת
משדרת את כל האצווה יחד). `create_bank_match()` ליצירה ידנית (one-to-one/one-to-many/
many-to-one/פיצול). `approve_bank_match()`/`reject_bank_match()` לאישור/דחייה.

**נתפס בביקורת עצמית (לפני הרצה חיה)**: `approve_bank_match()` בטיוטה הראשונה בדק רק
שסכום ההתאמות המאושרות **מול היעד** (אצוות מס"ב/תרומה) לא יעלה על סכום היעד - אבל לא
בדק שסכום ההתאמות המאושרות **מול אותה תנועת בנק עצמה** (מול כל היעדים ביחד) לא יעלה על
סכום התנועה. בלי הבדיקה הזו, אותה תנועת בנק הייתה יכולה "לכסות" גם אצוות מס"ב וגם תרומה
בו-זמנית ולהכפיל בפועל את הכסף. תוקן בגרסה שרצה בפועל (ר' `038` על הדיסק) - אין live
patch כי המיגרציה טרם רצה בפרויקט החי.

`039_stage12_reconciliation_exceptions.sql`: view `bank_reconciliation_exceptions`
(מחושב תמיד, לא טבלה - כמו `group_balances`), 5 סוגי חריגה (2 קריטיים: מס"ב ששודר/
תנועה מסווגת-מס"ב בלי התאמה מאושרת; 3 אזהרה: תרומה/תנועת-זכות-חשודה-כתרומה בלי התאמה,
תנועת חיוב לא מסווגת סופית).

`040_stage12_returns_integration.sql`: מרחיב את `enforce_payment_return_mutation()` הקיימת
(שלב 10, כבר רצה בפרויקט החי ב-033) בשני כללים: חסימת קישור תשלום חוזר עד שהחשבון החדש
אומת בפועל; חסימת סגירת החזרה (`resolved`) עד שהתשלום החוזר הותאם ואושר בפועל מול הבנק
(האצווה של שורת התשלום החוזר הגיעה ל-`bank_completed`).

`041_stage12_month_close.sql`: מוסיף `status_reason` ל-`financial_periods` (כבר רצה משלב
8, ALTER). מחליף את `set_financial_period_status()` (גם היא כבר רצה משלב 8) כך שלא ניתן
עוד לקבוע `'closed'`/לצאת מ-`'closed'` ישירות - רק דרך `close_financial_month()`/
`reopen_financial_month()` החדשות. `close_financial_month()` מריצה מחדש בשרת (לא סומכת
על מה שהוצג בלקוח) את עשרת התנאים שמחזירה גם `get_month_close_checklist()`, ואוספת את כל
הכשלים לחריגה אחת (לא עוצרת בראשון). **שים לב**: תנאי "התאמות בנק ממתינות" בצ'קליסט הוא
ברמת העמותה כולה, לא ממוקד לחודש הספציפי (כי התאמה בודדת לא תמיד קשורה לחודש אחד ברור) -
עלול לחסום סגירת חודש בגלל התאמה ממתינה שקשורה בפועל לחודש אחר. החלטה מכוונת לטובת
זהירות-יתר, לא באג - ראוי לדעת.

`038`-`041` טרם רצו בפרויקט החי - ללא live patch (הכל על הדיסק, יורץ פעם ראשונה).

**פריט לניקוי עתידי (לא טופל עכשיו - "ניקוי בסוף, לפני מסירה")**: לעמודה
`donations.bank_transaction_id` (שלב 9, `030`) אין FK constraint (bank_transactions עוד
לא היה קיים אז) ואף קוד/RPC לא משתמש בה - הקישור תרומה↔תנועת-בנק האמיתי היום הוא דרך
`bank_matches` (שלב 12, יעד גנרי). העמודה מיותרת ויש להסיר אותה בסבב הניקוי הסופי.

## שלב 13: 042-045 - ביקורות, בימות המשיח, מסמכים ומכתבים

`042_stage13_audits.sql`: `audits` (אירוע ביקורת עמותה+סניף+תאריך) + `audit_attendance`
(שורת "חוסר" לכל תלמיד, לפי מזהה - "לא מותאם" נשאר להחלטה). היבוא עצמו דרך מנוע היבוא
הכללי (שלב 5), לא צינור ייעודי - בדיוק כמו שהאפיון מבקש. `is_recurring` מחושב ב-commit
(אותו עיקרון כמו `talmud_errors`, שלב 6) - "חוזר" פשוט אומר "קרה כבר לפני כן באותה עמותה",
לא סף שרירותי.

`043_stage13_phone_lists.sql`: `phone_list_imports`/`phone_list_entries` - צינור ייעודי
(לא המנוע הכללי, כפי שהאפיון קורא לטבלאות בשם מפורש), אותו מבנה כמו `bank_import_batches`/
`rows`. סיווג pending/duplicate/invalid בזמן ניתוח הקובץ בצד לקוח; ההתאמה האמיתית
(matched/extra) מול תלמידים פעילים רק בשרת ב-commit. "חסר" (תלמיד פעיל בלי טלפון בקובץ)
לא מאוחסן - נשלף לפי דרישה.

`044_stage13_documents.sql`: `documents` - `document_type` טקסט חופשי (לא רשימה סגורה,
החלטה עסקית פתוחה). מסמך רגיש (`is_sensitive`) מוגן בהרשאת `view_sensitive` נפרדת, גם על
שורת הטבלה עצמה וגם על ה-Storage.

`045_stage13_letters.sql`: `letter_templates` + `letters` - מכונת מצבים
draft→approved→sent→answered/archived, נאכפת בטריגר (לא רק במסך) כדי ש"אישור אנושי חובה"
יישאר אמיתי גם אם AI יחובר בעתיד. אחרי `sent` אי אפשר לערוך את תוכן המכתב.

**נתפס בביקורת עצמית (לפני הרצה חיה)**: `audits` הייתה יכולה להיווצר/להתעדכן ישירות
כ-`completed` בלי שאף רשימת חסרים נקלטה בפועל (אותה מחלקת באג ה-INSERT-guard שכבר חזרה
כמה פעמים); ו-`documents_update`/ה-Storage select policy בדקו רק `documents.manage` בלי
להתחשב ב-`is_sensitive` - מי שיש לו manage בלי view_sensitive היה יכול "לכתוב בעיוורון"
על שורה/קובץ רגיש בלי לראות אותו בכלל. שניהם תוקנו לפני הרצה.

**נתפס בבדיקה חיה (לא ביקורת עצמית) - תיקון מתוקן ב-`046`**: כל 7 הטריגרים
`create trigger X_audit on TABLE` בקבצים 042-045 נכתבו עם `on TABLE` כפול (גם מיד אחרי
שם הטריגר וגם אחרי `after insert or update`) - שגיאת תחביר `syntax error at or near "on"`
בהרצה הראשונה של 042. תוקן ישירות בקבצים (עדיין לא רץ בהצלחה באף אחד מהם באותו רגע).

**נתפס בבדיקה חיה של שלב 13 - רגרסיה אמיתית מ-034, לא קשורה לשלב 13 עצמו**: יבוא רשימת
חסרים לביקורת נכשל עם "אצוות יבוא חדשה תמיד נוצרת בסטטוס 'הועלה'" - כי `034` (הביקורת
הרוחבית לקראת שלב 11) החמיר את `enforce_import_batch_commit()` כך ש-INSERT ישיר על
`import_batches` חייב `status='uploaded'` בדיוק, אבל `lib/importBatches.ts`
(`createImportBatch`, המשותפת למרכז שגיאות/זכאות/ביקורות) יוצרת את השורה ישר במצב
`'previewed'` (הקובץ כבר נותח בצד לקוח). מהרגע ש-034 רץ בפרויקט החי, **כל** יבוא דרך
מרכז השגיאות ומסך הזכאות היה שבור בפועל - לא נתפס כי הביקורת החוזרת אחרי 034 בדקה RLS/
ספר תנועות/גבולות הרשאה, לא יבוא-מאפס דרך המסכים האלה עצמם. 013/034 עודכנו בדיסק להתיר
גם `'previewed'` בהיווצרות; `046_patch_live_import_batch_previewed_insert.sql` הוא ה-patch
לפרויקט הקיים. **מסקנה**: תיקון guard-trigger שנכתב מול RPC אחד (`commit_errors_batch`
וכו') חייב להיבדק גם מול איך שהלקוח *באמת* יוצר את השורה מלכתחילה, לא רק מול "מה ה-RPC
עושה" - "האצווה תמיד נוצרת X" הוא הנחה שצריך לוודא מול הקוד בפועל, לא להניח.

## שלב 15: 052-055 - ניהול נתוני דמו ומעבר לנתוני אמת, 2026-07-24

`052_stage15_demo_data.sql`: `demo_batches` (מעקב אחר כל חבילת דמו) + `create_demo_batch()`
- שרשרת סינתטית אחת (עמותה→סניף→2 קבוצות→3 תלמידים→זכאות→עמלה→תרומה/חלוקה→מס"ב→בנק→
התאמה/החזרה), כל שורה `is_demo=true`+`demo_batch_id`, תחת עמותת "DEMO" נפרדת בלבד.
`preview_demo_batch_deletion()`/`delete_demo_batch()` - מחיקה בסדר תלות מפורש (ילדים לפני
הורים, לא CASCADE), מאומת מול ה-FK-ים האמיתיים בכל טבלה.

**נתפס בביקורת עצמית (לפני הרצה חיה) - שלוש פעמים באותה מחלקת באג**: `students`
(`enforce_student_status_transition`), `donations` (`enforce_donation_mutation_guard`),
ו-`distribution_batches` (`enforce_distribution_batch_mutation`) - לכל אחת יש בדיקת
INSERT שדוחה סטטוס שאינו ברירת המחדל **בלי תנאי** (הדגל `app.allow_*_status_change`
משפיע רק על UPDATE). נכתב במקור כ-INSERT ישיר במצב הסופי הרצוי - תוקן ל-insert כברירת
מחדל ואז update עם הדגל, לכל שלושת הטבלאות. גם נמצא ותוקן שריד SQL מסוכן ממש (DELETE לא
מוגבל על `student_bank_accounts` שהיה יכול למחוק שורות אמת) - הוסר לפני הרצה.

`053_patch_live_demo_data_exclusion.sql`: `get_ops_dashboard_counts()` ו-`unified_exceptions`
(שתיהן כבר רצות חי מ-048, שלב 14) הן הפונקציות היחידות שמצטברות גלובלית על פני כל
העמותות בלי בורר עמותה מחייב - עודכנו לא לכלול נתוני דמו. הדשבורד התפעולי מסונן לפי
`is_demo` של כל טבלה ישירות (כי `students` עצמה לא קשורה ישירות לעמותה); `unified_exceptions`
עטוף ב-join ל-`organizations.is_demo` (כי `bank_reconciliation_exceptions`, אחד ממקורות
האיחוד, הוא view שלא חושף is_demo בעצמו) - בכוונה לא מסננת לפי is_demo של כל שורה בנפרד,
כדי שהמנגנון יישאר תקף גם אם מחולל הדמו העתידי ייגע בטבלאות נוספות.

`054_stage15_master_data_import.sql`: אשף "מעבר לנתוני אמת" - פרופיל יבוא ייעודי
(`master_data`) על גבי מנוע היבוא הכללי (שלב 5), commit ספציפי-דומיין
(`commit_master_data_import_batch`) שעושה find-or-create בכל רמה (עמותה לפי מספר/שם,
סניף לפי קוד, ראש קבוצה לפי שם+טלפון, קבוצה לפי שם) - אף פעם לא `is_demo=true`, כך
שאין דרך מבנית לערבב אמת ודמו באותה אצווה.

**נתפס בבדיקה חיה (לא ביקורת עצמית) - פער סכימה ישן משלב 8, לא קשור לשלב 15 עצמו**:
`create_demo_batch()` נכשלה בהרצה החיה הראשונה שלה עם `column "is_demo" of relation
"eligibility_financial_results" does not exist` - הטבלה נבנתה ב-`026` (שלב 8) בלי
`is_demo`/`demo_batch_id` בכלל, בניגוד לכל שאר הטבלאות מאותו שלב ואילך (הושמט אז, לא
נתפס כי שום דבר לפני שלב 15 לא ניסה לתייג שורה בטבלה הזו כדמו). כל הפעולה חזרה לאחור
בטרנזקציה אחת (הפונקציה כולה) - לא נוצרה אף שורה חלקית. `026` עודכן בדיסק עם שתי העמודות;
`055_patch_live_efr_is_demo.sql` הוא ה-patch לפרויקט הקיים (`ALTER TABLE ... ADD COLUMN`).
**מסקנה**: הנחה "כל טבלה מקבלת is_demo/demo_batch_id" (שתועדה כעובדה מבוססת בתחילת השלב)
עדיין דרשה אימות בפועל מול כל טבלה שהמחולל נוגע בה, לא רק הסתמכות על תיעוד קודם - בדיוק
כמו הלקח החוזר עם guard triggers: תמיד לקרוא את הסכימה בפועל, לא להניח לפי דפוס.

**נתפס בבדיקה חיה (הרצה שנייה, אחרי 055) - אותה מחלקת באג guard-trigger, בגרסה על
טבלת האב**: `create_demo_batch()` נכשלה שוב עם "ניתן לערוך שורות חלוקה רק כאשר האצווה
בטיוטה (סטטוס נוכחי: locked_for_masav)" - הפונקציה נעלה את `distribution_batches`
ל-`locked_for_masav` *לפני* שיצרה את `distribution_lines` שלה.
`enforce_distribution_lines_mutation()` (031) חוסמת כל insert/update/delete על
`distribution_lines` כשסטטוס האצווה שלהן אינו 'draft' - בלי שום דגל עוקף בכלל (לא כמו
שאר המקרים מהמחלקה הזו: כאן הבדיקה היא על טבלת *האב*, לא על הטבלה עצמה, ואין דרך לעקוף
אותה חוץ מסדר פעולות נכון). כל הפעולה חזרה לאחור בטרנזקציה אחת. תוקן ב-`052` (שורות
החלוקה נוצרות לפני הנעילה, לא אחריה); `056_patch_live_create_demo_batch_distribution_order.sql`
הוא ה-patch לפרויקט הקיים (re-create מלא של הפונקציה). **מסקנה נוספת**: "קרא את הטריגר
בפועל" לא מספיק כשיש שרשרת טבלאות-בת - צריך לבדוק גם טריגרים על טבלאות *תלויות*, לא רק
על הטבלה שמשנים ישירות.

**נתפס בבדיקה חיה (ניסיון מחיקה ראשון) - סוג חדש של guard, לא אותה מחלקת באג**:
`delete_demo_batch()` נכשלה עם "group_ledger_entries is append-only: DELETE not allowed" -
`block_ledger_mutation()` (026, שלב 8) חוסמת UPDATE/DELETE על ספר התנועות **ללא שום דגל
עוקף בכלל** (לא כמו שאר הבאגים מהשלב הזה - כאן זו לא בדיקת סטטוס עם דגל שנשכח, אלא חסימה
מוחלטת ומכוונת: ספר תנועות אמיתי לעולם לא נמחק/נערך, תיקון הוא רק שורה נגדית). הוספת דגל
`app.allow_demo_ledger_delete` חדש - ל-DELETE בלבד (לא UPDATE - גם בדמו, תיקון אמור להישאר
שורה נגדית; רק מחיקה מלאה של חבילת דמו היא המקרה החדש שדורש חריגה), מופעל אך ורק בתוך
`delete_demo_batch()`, על שורות עם demo_batch_id תואם בלבד - לעולם לא נוגע בספר תנועות
אמיתי. 026/052 עודכנו בדיסק; `057_patch_live_ledger_delete_and_demo_delete.sql` הוא ה-patch
לפרויקט הקיים. **מסקנה**: guard triggers "ללא תנאי" מגיעים בשתי צורות שונות - בדיקת
סטטוס-על-הטבלה-עצמה-או-האב (עם דגל שנשכח, נתפס כמה פעמים לעיל) לעומת חסימה מוחלטת
ומכוונת-בעיצוב (בלי דגל בכלל, כי "לעולם" זו הדרישה העסקית) - הסוג השני דורש להוסיף דגל
חדש במפורש, לא רק "להיזכר" בדגל קיים.

**נתפס בבדיקה חיה (ניסיון מחיקה שני, אחרי 057) - אותו guard כמו ב-create_demo_batch, הפעם
בכיוון ההפוך**: `delete_demo_batch()` נכשלה עם אותה הודעה בדיוק כמו ב-`create_demo_batch`
("ניתן לערוך שורות חלוקה רק כאשר האצווה בטיוטה") - הפעם ב-DELETE, לא INSERT.
`enforce_distribution_lines_mutation()` (031) חוסמת גם DELETE על `distribution_lines`
כשהאצווה שלהן אינה 'draft', לא רק insert/update - ומחיקת חבילת דמו מנסה למחוק את השורות
בזמן שהאצווה עדיין `locked_for_masav` (המצב שאליו היא ננעלה ב-`create_demo_batch`).
תוקן ע"י החזרת האצווה ל-'draft' (עם הדגל הקיים) ממש לפני מחיקת השורות - האצווה עצמה
נמחקת מיד אחרי, כך שאין מצב ביניים שנשאר בסטטוס שגוי. `052` עודכן בדיסק;
`058_patch_live_delete_demo_batch_distribution_lines.sql` הוא ה-patch לפרויקט הקיים.
לאחר תיקון זה בוצע סריקה שיטתית של **כל** הטריגרים בסכימה שמופעלים ב-DELETE (`grep` רוחבי
על "before/after ... delete", לא רק "before delete" - `distribution_lines` פוספס בסריקה
ראשונה כי הטריגר שלו כתוב `before insert or update or delete`, לא `before delete` בנפרד) -
נמצאו רק 4 טבלאות בכל הסכימה עם טריגר כזה: `audit_events`, `payment_calculation_versions`
(שתיהן לא נוגעות ל-`delete_demo_batch`), `group_ledger_entries` ו-`distribution_lines`
(שתיהן כבר תוקנו). **מסקנה**: כשמחפשים "אילו טבלאות חוסמות DELETE", לחפש `delete` בכל
מקום בהגדרת הטריגר, לא רק מיד אחרי `before` - טריגר משולב (insert/update/delete יחד)
נראה כמו טריגר INSERT/UPDATE רגיל בסריקה שטחית.

**נתפס בבדיקה חיה של אשף המעבר לנתוני אמת - אותה מחלקת באג, טבלה חדשה**:
`commit_master_data_import_batch()` (054) נכשלה עם "קבוצה נוצרת תמיד בלי ראש קבוצה - שיוך
ראשון נעשה דרך reassign_group_leader() לאחר היצירה" - ניסתה להכניס group_leader_id ישירות
ב-INSERT של groups. `enforce_group_leader_transition()` (010, מ-`010_fix_group_leader_history.sql`)
חוסמת זאת ללא תנאי, כדי שכל שיוך ראש-קבוצה ישאיר שורת `group_leader_assignments` היסטורית
תואמת (לא רק `groups.group_leader_id` כ-FK רגיל). תוקן: קבוצה נוצרת בלי ראש; אם יש ראש
קבוצה בשורה, נוצרת שורת `group_leader_assignments` ואז `groups.group_leader_id` מתעדכן
עם הדגל `app.allow_group_leader_change` (הקיים, זהה למה ש-`reassign_group_leader()` עצמה
משתמשת בו) - בלי לקרוא לפונקציה עצמה, כדי לא לדרוש מהמשתמש הרשאת `groups.manage` נוספת
מעבר ל-`master_data_import.perform`. 054 עודכן בדיסק; `059_patch_live_commit_master_data_group_leader.sql`
הוא ה-patch לפרויקט הקיים. **מסקנה**: כל טבלה עם עמודת "מזהה הבעלים/האחראי הנוכחי"
(group_leader_id, וכו') ראויה לחשד מראש כמועמדת לאותה מחלקת guard - יש לבדוק את הטריגר
בפועל *לפני* הכתיבה הראשונה, לא רק אחרי כישלון.

## שלב 16: 060 - תיקון אבטחה קריטי, מספרי חשבון בנק ואסמכתאות תרומה בטקסט גלוי, 2026-07-24

**נתפס בביקורת אבטחה (subagent, לא בדיקה חיה) - קריטי, לא תיאורטי**: `organization_bank_accounts_select`/
`student_bank_accounts_select`/`donations_select` (006/009/030, משלבים 3/4/9) התירו SELECT
ישיר על הטבלה הבסיסית לכל מי שיש לו `area_ops`/`area_finance` - כולל את עמודת המספר הגולמי
עצמה (`account_number`/`donor_reference`). המיסוך (`mask_account_number()` + view + RPC
`reveal_*` המוגן ב-`bank_accounts.view_sensitive` ומתועד ביומן) הוגן **רק** ב-view וב-RPC,
מעולם לא ב-RLS על הטבלה - כל מי שידע לקרוא ישירות מהטבלה (למשל דרך PostgREST ישיר) עקף
את המיסוך לגמרי, בלי שום תיעוד ביומן. **אומת שזה קרה בפועל, לא רק תיאורטית**: `ReportsScreen.tsx`
(דוח "תרומות") ו-`BankMatchingScreen.tsx` (בורר תרומה להתאמת בנק) שניהם קראו את `donor_reference`
הגולמי ישירות מהטבלה בפועל, בקוד לקוח קיים - לא רק חור RLS תיאורטי.

**תיקון**: כל שלושת ה-SELECT policies דורשים עכשיו `bank_accounts.view_sensitive` (בדיוק
כמו ה-RPCs). כדי שמי שאין לו view_sensitive ימשיך לראות שורות (ממוסכות) דרך ה-view כרגיל,
שלושת ה-views (`organization_bank_accounts_view`/`student_bank_accounts_view`/`donations_view`)
הפסיקו לרוץ כ-`security_invoker=true` ורצות עכשיו כ-owner - עם שכפול מפורש של כלל הראייה
המקורי (`area_ops`/`area_finance`) בתוך ה-WHERE של כל view עצמו, כדי שלא ידלוף שום דבר
מעבר למה שה-RLS המקורי כבר איפשר. `ReportsScreen.tsx`/`BankMatchingScreen.tsx` עודכנו
לקרוא מ-`donations_view`/`donor_reference_masked` במקום מהטבלה. 005/006/008/009/030
עודכנו בדיסק; `060_patch_live_sensitive_data_rls_fix.sql` הוא ה-patch לפרויקט הקיים
(כולל `alter view ... reset (security_invoker)` מפורש - `create or replace view` לבדו
לא מובטח לאפס reloption קיים). **מסקנה**: "המיסוך קיים ב-view" אינו מספיק כשה-RLS על
הטבלה הבסיסית עצמה עדיין מתיר SELECT רחב יותר מהכוונה - חובה לוודא שהגישה הישירה לטבלה
דורשת בדיוק את אותה הרשאה כמו הדרך ה"רשמית" לקבל את הערך הרגיש, אחרת ה-view הוא רק
עיטור ולא בקרה אמיתית.

## שלב 17: 062-063 - Audit סופי מול האפיון, שני פערים קטנים שנסגרו, 2026-07-24

**נתפס בביקורת traceability מול תנאי הקבלה הסופיים (22 תנאים, לא בדיקה חיה)**: שני
פערים קטנים, שניהם לא דרשו החלטה עסקית ולכן תוקנו ישירות (בניגוד לארבעת חסמי ה-Production
הכספי, שנשארים פתוחים בכוונה - ר' סעיף נפרד למטה).

1. **"נתוני דמו אינם יוצאים לאינטגרציות"**: `create_talmud_export()` (017) לא חסמה במפורש
יצוא מעמותת דמו - ההגנה היחידה הייתה מוסכמתית (משתמש פשוט לא בוחר בעמותת ה-DEMO). נוספה
בדיקה מפורשת הדוחה יצוא אם `organizations.is_demo = true`. 017 עודכן בדיסק;
`062_patch_live_talmud_export_demo_guard.sql` הוא ה-patch לפרויקט הקיים.

2. **עקביות ב-commit RPCs**: `commit_master_data_import_batch` (054/059) ו-
`commit_audit_attendance_batch` (042) הסתמכו רק על סינון שורות `status='valid'` כדי
למנוע עיבוד כפול - בטוח בפועל, אבל בלי בדיקת סטטוס-אצווה מוקדמת ומפורשת כמו
`commit_bank_import_batch` (036, שלב 11). שתי הפונקציות עודכנו להוסיף אותה בדיקה
(`status not in ('uploaded','previewed') -> raise exception`). 042/054 עודכנו בדיסק;
`063_patch_live_commit_rpcs_batch_status_guard.sql` הוא ה-patch לפרויקט הקיים.

**תוצאת ה-Audit המלא (22 תנאי קבלה)**: 20 Pass, 1 Partial (סעיף 15, תוקן כאן ל-Pass),
1 Missing כתכונת-מערכת נפרדת (סעיף 22, "יומן החלטות" - הפתרון הנכון הוא מסמך תיעוד,
לא טבלה/מסך חדש - ר' דוח ה-Go/No-Go הסופי). לא נמצא אף ממצא אבטחה קריטי נוסף (הממצא
הקריטי היחיד תוקן כבר בשלב 16, מיגרציה 060). ארבעת חסמי ה-Production הכספי (פורמט מס"ב,
כללי עמלה/עיגול, נוסחת 90%/10%, שרשרת חודש מלאה על נתוני אמת) נשארים פתוחים במפורש,
לא הומצא/הוחלט כלל עסקי במקומם - זהו השלב האחרון בתוכנית הבנייה.

## 064 - ניקוי סופי לפני מסירה ללקוח, 2026-07-24

לבקשת Chani, אחרי סיום שלב 17: ניקוי מלא של כל נתוני הבדיקה/הפיתוח שהצטברו לאורך כל
הפרויקט, לקראת התחלת עבודה עם נתוני אמת. `TRUNCATE` (לא `DELETE`) על 44 טבלאות -
נבחר במפורש כי TRUNCATE לא מפעיל טריגרים ברמת-שורה, ולכן עוקף באופן טבעי את כל
טריגרי ה-append-only/guard שנבנו במהלך הפרויקט (ספר תנועות, יומן פעולות, גרסאות רטרו
וכו') בלי לגעת באף אחד מהם ובלי לחשב סדר תלות ידני - כל הטבלאות מתרוקנות יחד באותה
פקודה אחת. **חד-פעמי ובלתי הפיך** - לא migration רגילה בתבנית הקבועה של הפרויקט (אין
"תוקן על גבי" - זו פעולת תחזוקה, לא תיקון קוד).

**נשארות** (הגדרות מערכת + משתמשים אמיתיים, אומת שאין להן אף FK לטבלה שנמחקת):
`roles`, `permissions`, `role_permissions`, `profiles`, `user_permissions`,
`import_profiles`, `export_profiles`, `bank_import_profiles`, `bank_transaction_types`,
`study_codes`, `letter_templates`.

**נמחקות**: כל 44 הטבלאות הנותרות (ר' `064_final_data_reset_before_client_handoff.sql`
לרשימה המלאה) + מטא-דאטה של קבצים שהועלו לבדיקה בכל דלי ה-Storage.

## שלב 18: 065 - מודול "ניהול משימות" - ליבה: מסד נתונים, הרשאות ו-RLS, 2026-07-26

מודול חדש שלם, לפי `אפיון_מקצועי_משימות_ותזכורות_עולם_התורה_V2.docx` (גרסה 2.0) ו-
`TASKS_MODULE_PLAN.md`. אזור רביעי במערכת (`area_tasks`), לצד תפעול/כספים/ניהול.

- 14 הרשאות חדשות (`area_tasks/access` + 13 `tasks.*`) - הענקה אוטומטית רק ל-system_admin,
  בדיוק כמו כל שאר ההרשאות במערכת. שאר התפקידים/המשתמשים מוגדרים ידנית ממסך
  `/admin/users` הקיים - הוא גנרי לגמרי (קורא מ-`permissions` דינמית), אז ההרשאות
  החדשות מופיעות שם אוטומטית בלי לגעת ב-UI.
- `teams`/`team_members` - קונסטרוקט חדש שלא קיים היום בשום מקום אחר במערכת (המערכת
  הקיימת מכירה רק תפקיד + חריגות פר-משתמש, לא "צוותים"). נזרעו 3 צוותים לפי השימוש
  בפועל שתיארה Chani: אורי (מנהל), כללי, משה - תפעול. חברות בפועל תתווסף ב-UI (שלב 19).
- `tasks` + טבלאות-בת: `task_owners`/`task_teams` (many-to-many), `task_watchers`
  (צופה/משתתף), `task_checklist_items`, `task_comments`, `task_attachments`, `task_links`
  (קישור פולימורפי לישויות קיימות - entity_id בלי FK אמיתי, מכוון), `task_history`
  (append-only, כמו `audit_events`, נכתב רק מטריגרים).
- **אין `is_demo`/`demo_batch_id` בשום טבלה במודול** - דרישה מפורשת באפיון. המודול לא
  נכנס לרשימת הטבלאות ב-`delete_demo_batch()`/`preview_demo_batch_deletion()` (052),
  ואסור להוסיף אותו לשם בעתיד.
- `can_view_task()`/`can_edit_task()` - נקודה מרכזית אחת לכלל הראות (view_all/view_team/
  view_assigned), כדי לא לשכפל את התנאי המשולש בשמונה טבלאות שונות. זו ה-RLS הכי
  מורכבת שנבנתה עד היום בפרויקט.
- `enforce_task_status_transition()` - טריגר (לא RLS) לאכיפת `tasks.complete`/
  `tasks.reopen` על מעברי סטטוס ספציפיים, באותו דפוס בדיוק כמו
  `enforce_group_leader_transition`/`enforce_distribution_lines_mutation` בשאר הפרויקט -
  השוואת OLD מול NEW לא ניתנת לביטוי יחיד ב-RLS `using`/`with check`.
- Storage bucket חדש `task-attachments`, מתואם עם ה-RLS על שורת `task_attachments` עצמה
  (לא רק בדיקת הרשאה כללית) - אותו עיקרון שתוקן בעבר ב-`organization-documents`.

**עדיין לא בשלב הזה** (לפי `TASKS_MODULE_PLAN.md`): מסכים/ניווט (שלב 19), מנוע תזכורות
ומשימות חוזרות (שלב 20), קליטת WhatsApp (שלב 21), תבניות/Kanban/לוח שנה (שלב 22).

## שלב 19: 066 - רשימת משתמשים מינימלית לשיוך משימות, 2026-07-26

RLS על `profiles` (004) מתיר לכל משתמש לראות רק את השורה שלו, אלא אם יש לו
`users.manage`. שיוך אחראים/צוותים/צופים במסכי המשימות דורש מכל משתמש עם גישה לאזור
המשימות לראות רשימת שמות עמיתים - בלי `users.manage`. `list_assignable_users()` חושפת
תת-קבוצה מינימלית ולא רגישה (`id`+`full_name` בלבד, לא email/status/role) למי שיש לו
`area_tasks.access`, בלי לפרוץ את ה-RLS הכללי של `profiles` עצמה. אותו עיקרון כמו
`my_permissions()` - פונקציית SECURITY DEFINER צרה שחושפת בדיוק את מה שנדרש, לא יותר.

## שלב 20: 067-068 - תזכורות ומשימות חוזרות, 2026-07-26

**067** - `task_reminders` (תאריך מוחלט או יחסי ליעד, ליעד ספציפי או לכל האחראים,
Snooze, is_dismissed). "מנוע שרתי" (סעיף 13 באפיון) מתפרש כאן כחישוב "הגיע-הזמן" תמיד
ב-SQL מתאריך מאוחסן (לא מ-setTimeout בצד לקוח) - אין באפיון דרישה לערוץ שליחה חיצוני
(מייל/Push), רק לערוץ בתוך המערכת, אז לא נבנה דיימון שליחה. טריגר על `tasks.due_date`
מעדכן אוטומטית תזכורות יחסיות כשתאריך היעד משתנה - נתפס תוך כדי תכנון, לא בבדיקה חיה.
`snooze_reminder()`/`dismiss_reminder()` הם RPC (לא RLS UPDATE ישיר) כדי לאפשר גם
למי שהתזכורת מיועדת לו אישית (גם בלי `tasks.edit`) לנהל אותה.

`task_recurrence_rules` + `task_recurrence_owners`/`teams` (תדירות יומית/שבועית/חודשית/
שנתית/מותאם, מדיניות "מופע חדש גם אם הקודם לא הושלם"). `next_recurrence_date()` מיישר
`day_of_month` לתאריך התקף האחרון בחודש (31 בפברואר -> 28/29). `generate_due_recurrences()`
היא ה"מנוע" בפועל - כותבת `tasks`/`task_owners`/`task_teams`/`task_reminders` חדשים,
עם מניעת כפילות דרך `last_generated_date` על הכלל עצמו.

**068** (קובץ נפרד בכוונה) - `pg_cron` שמריץ את `generate_due_recurrences()` יומית ב-06:00.
הופרד מ-067 בגלל הלקח מ-064 (חלק אחד שנכשל מחזיר את כל הטרנזקציה) - אם `pg_cron` חסום
בתוכנית ה-Supabase, רק 068 ייכשל, וכל טבלאות/הרשאות/פונקציות התזכורות והחזרות (067)
כבר יהיו קיימות ותקינות.

## שלב 21: 069 - קליטת משימות מ-WhatsApp, 2026-07-26

אין Supabase CLI/Docker בסביבת הפיתוח (שורה 3 למעלה) ולכן אי אפשר לפרוס Edge Function
מכאן. הפתרון: `ingest_whatsapp_message()` היא פונקציית SQL רגילה, קריאה ישירות דרך
ה-REST API האוטומטי של Supabase (`POST /rest/v1/rpc/ingest_whatsapp_message` עם מפתח
ה-anon) - בלי Edge Function בכלל. מקבלת שדות כבר מנורמלים, לא JSON גולמי של ספק ספציפי -
המיפוי המדויק מה-Webhook של הספק שייבחר (סעיף 22 באפיון, עדיין החלטה פתוחה) לקריאה הזו
יילמד בהדרכה נפרדת כשהספק ייקבע.

אבטחה: זהו endpoint חיצוני אנונימי (אין משתמש מחובר) - ה-RLS הרגיל לא רלוונטי. הגנה:
`whatsapp_webhook_config` (טבלת singleton, RLS מופעל בלי אף policy = deny-all מוחלט
גם ל-authenticated, ערך מוזן ידנית ע"י Chani ב-SQL Editor) + בדיקת secret בתוך הפונקציה
עצמה (SECURITY DEFINER). הודעה מקבוצה לא מאושרת/לא פעילה נזרקת בשקט (לא שגיאה - Webhook
לא אמור להיכשל בגלל זה). Dedup דרך `unique(group_id, source_message_id)`.

`convert_whatsapp_message_to_task()` - טרנזקציה אחת (יצירת משימה + שיוך אחראים/צוותים +
סימון ההודעה כ-converted + רישום `task_history` מסוג `converted_from_whatsapp`, שהוגדר
כבר בשלב 18 ולא נוצל עד עכשיו). `tasks.source_whatsapp_message_id` (ALTER TABLE) משלים
את זיקת "מקור המשימה" שנפתחה בשלב 18.

**לפני שמחברים ספק אמיתי**: יש להזין את ה-secret פעם אחת -
`insert into whatsapp_webhook_config (secret) values ('...') on conflict (id) do update set secret = excluded.secret;`
- ערך אקראי וסודי, לא סיסמה קיימת. עד אז הפונקציה קיימת אך תדחה כל קריאה (`v_stored_secret is null`).

## שלב 22: 070 - תבניות משימה, 2026-07-26

`task_templates` + `task_template_checklist_items`. "אוטומציות" מסעיף 16 באפיון (אירוע
מקור **או** לוח זמנים) נדחו במכוון: לוח-זמנים כבר מכוסה במלואו ע"י משימות חוזרות (שלב
20), ו"אירוע מקור" גנרי (טריגר על אירוע שרירותי במערכת) דורש תשתית triggers רוחבית
על כל הטבלאות שלא הוגדרה באפיון בפירוט מספיק כדי לבנות בלי להמציא כללים - זו לא
השמטה בטעות, ר' TASKS_MODULE_PLAN.md.

## שלב 22: 071 - תיקוני אבטחה מביקורת ייעודית על מודול המשימות, 2026-07-26

לפני שהמודול נחשב גמור, subagent ביצע ביקורת אבטחה אדוורסרית על migrations 065-070
(אותו תהליך כמו שלב 16, שמצא אז את הבאג הקריטי במספרי חשבון בנק). 4 ממצאים אמיתיים,
כולם patch על מיגרציות שכבר רצו נגד הפרויקט החי (create or replace/revoke בלבד):

1. **`generate_due_recurrences()` (067) בלי `revoke execute`** - כמו כל פונקציה פנימית
   אחרת במערכת (`insert_audit_event` וכו'), הייתה חסרה לה החסימה המפורשת. Postgres
   מעניק EXECUTE ל-PUBLIC כברירת מחדל - כל authenticated (ואולי anon) יכול היה לקרוא
   לה ישירות דרך REST ולכפות יצירת מופע חזרה מוקדם, בלי `tasks.create`.
2. **`ingest_whatsapp_message()` (069) שמרה `attachment_url` בלי בדיקת סכמה** - קישור
   `javascript:...` היה מוצג כ-`<a href>` אמיתי ב-`TasksWhatsAppScreen` (React לא מנקה
   href) ומופעל בלחיצה - XSS מאוחסן. כעת רק `http`/`https` מתקבל בשרת (כל דבר אחר
   מנוקה ל-null, לא חוסם את ההודעה כולה), ועם הגנת עומק זהה גם בצד לקוח.
3. **`convert_whatsapp_message_to_task()` (069) שיוכה אחראים/צוותים בלי לבדוק
   `tasks.assign_users`/`assign_teams`** - עקפה את ה-RLS הרגיל של אותן טבלאות (כי היא
   SECURITY DEFINER). לא נוצל בפועל (רק system_admin מחזיק `tasks.convert_whatsapp`
   כרגע), אבל הרשאה עתידית בלי ההרשאות הנלוות הייתה עוקפת אותן.
4. **מדיניות Storage ל-`task-attachments` (065) בדקה רק `area_tasks.access`**, לא את
   המשימה הספציפית שמקודדת בנתיב הקובץ - אפשר היה להעלות קובץ תחת נתיב של משימה שלא
   רואים (בזבוז אחסון, לא דליפת מידע - קריאה עדיין חסומה). כעת נבדק `can_edit_task`
   על ה-UUID שבתחילת הנתיב.

**מסקנה חוזרת**: כמו בשלב 16, ביקורת ייעודית תפסה בעיות אמיתיות שלא נראו בזמן הכתיבה
הרגילה - הדפוס של subagent אדוורסרי לפני שמכריזים על שלב/מודול "גמור" ממשיך להוכיח
את עצמו.

## שלב 22: 072 - זיהוי אוטומטי של הודעת WhatsApp במבנה קבוע, 2026-07-26

לפי יומן ההחלטות (Chani, סעיף 4): "כן, אם אפשרי. גם מה שלא יסווג יכנס עם כל הטקסט".
`parse_whatsapp_structured_message()` מזהה מבנה `משימה: X | אחראים: Y | צוות: Z |
יעד: W` (סדר שדות חופשי, "|" מפריד, בדיוק כמו הדוגמה באפיון §15.5). זיהוי "יעד" תומך
רק ב-3 פורמטים מפורשים (ISO, DD/MM/YYYY, שמות ימים בעברית כמו "יום חמישי") - לא NLP
חופשי, כדי לא ליצור תאריך שגוי בטעות. `ingest_whatsapp_message()` (create or replace)
עכשיו מנסה לפרש כל הודעה נכנסת; אם זוהתה - יוצרת משימה ומסמנת `converted` מיד; אם לא -
נשארת `pending` בדיוק כמו קודם (ללא שינוי בהתנהגות הקיימת).

## שלב 22: 073 - תיקוני תקינות מביקורת ייעודית שנייה (לוגיקה, לא אבטחה), 2026-07-26

אחרי ביקורת האבטחה (071), subagent שני עבר על כל המודול בחיפוש אחרי באגי לוגיקה. 4
נמצאו בקוד הצד-לקוח (תוקנו ישירות, לא דורשים מיגרציה): (1) יוצר משימה בלי לסמן את עצמו
כאחראי לא ראה את המשימה שהוא עצמו יצר (`can_view_task` דורש היות אחראי/צופה/צוות) -
`createTask()` ב-`api.ts` מוסיפה עכשיו את היוצר לרשימת האחראים תמיד. (2) העתקת
Checklist מתבנית בזמן יצירת משימה לא בדקה שגיאות (Supabase-js לא זורק exceptions) -
כשלון היה שקט לגמרי. (3)-(4) חישובי תאריך "היום"/"יום" דרך `toISOString().slice(0,10)`
זזים ב-UTC, לא שעון מקומי - בישראל (UTC+2/3) זה גורם להזזת יום אחד בלוח השנה, ולשיבוץ
הפוך בין "להיום"/"באיחור" בין השעות 00:00-03:00. נוסף `dateUtils.ts` משותף
(`localDateIso`/`todayIso`/`addDaysIso`) והוחלף בכל מקום שהיה בו את התבנית הבעייתית.

ב-DB: `resolve_relative_reminder()` (067) סבלה מאותה בעיית אזור זמן - "0 ימים לפני
היעד" התפרש כחצות UTC (2-3 לפנות בוקר בישראל), לא חצות מקומי. תוקן עם `at time zone
'Asia/Jerusalem'` מפורש.

**נמצא ותועד, לא תוקן (החלטת מוצר, לא באג)**: `generate_due_recurrences()` מתקדמת
מופע אחד בכל הרצת cron - אם ה-cron מושהה כמה ימים ברציפות, אין "השלמה" של הפער, רק
המשך קדימה מנקודת ההשעיה. סביר לרוב השימוש, אבל שווה לדעת.

## שלב 23: 074 - תשתית משיכה אוטומטית מהבנק (Open Banking), 2026-07-29

לפי בקשת Chani: "מעוניינת שהתוכנה תמשוך מהבנקים כל לילה את התנועות האחרונות... תבדוק
שאין כפילות מחד ואין פספוס תנועות מאידך... תסווג לקטגוריות... ותפנה תשומת לב לתנועות
לא מזוהות". מתחילים עם בנק לאומי (יש שם ודאות חיבור API רשמי - ר' יומן החלטות), אך
התשתית כללית לכל בנק. Chani עדיין לא פנתה לבנק לקבלת פרטי חיבור בפועל בעת כתיבת
המיגרציה - נבנה הכל כך שרק צריך למלא API key/פרטי חיבור במקום אחד (`api/bank-sync.js`)
כשיתקבלו.

**למה לא נעשה שימוש ב-3 ה-RPCs הקיימים** (`commit_bank_import_batch`,
`suggest_transaction_types`, `suggest_masav_matches`, כולם משלבים 11-12) **ישירות**:
כולם דורשים `has_permission()` שמניח `auth.uid()` אמיתי - אין session אמיתי בהרצת
Vercel Cron (בדיוק כמו `generate_due_recurrences()` משלב 20). `ingest_bank_transactions_batch()`
החדשה משכפלת את הלוגיקה הדרושה משלושתן (staging+דדופ לפי fingerprint זהה
`compute_bank_fingerprint()` בדיוק לחישוב ה-JS הקיים, סיווג אוטומטי דרך
`match_recognition_rule()` הבלתי-מוגן, הצעת התאמות מס"ב באותה שאילתה), תחת הגנת
secret משותף (`bank_sync_config`, RLS ללא אף policy - דפוס זהה ל-`whatsapp_webhook_config`
משלב 21) במקום `has_permission()`.

**זיהוי פער** (`gap_detected`) הוא היוריסטי במפורש, לא ודאי: (1) פער-תאריכים בין
התנועה האחרונה שנקלטה בהרצה קודמת לתנועה החדשה המוקדמת ביותר, או (2) אפס תנועות
חדשות למשך יותר מ-3 ימים רצופים כשהמשיכה פעילה. שני המקרים מוצגים כהתראה לבדיקה
ידנית (מסך "משיכה אוטומטית מהבנק" + מרכז חריגות), לא נקבעים כוודאות.

**`list_due_bank_sync_accounts()`** נוספה בנפרד מ-`ingest_bank_transactions_batch()`:
הפונקציה ב-Vercel לא יכולה לקרוא `organization_bank_accounts` ישירות (RLS דורש
`has_permission()`) כדי לדעת אילו חשבונות אמורים לרוץ היום ומה מספר החשבון/סניף שלהם
(דרוש לקריאת ה-API בפועל) - נחשפת דרך RPC נפרד תחת אותו secret, ומחזירה רק את השדות
הדרושים לכך, לא את כל הטבלה.

**`log_failed_bank_sync_run()`** נוספה כדי שכישלון משיכה בפועל מהבנק (המצב הקבוע
כרגע, כל עוד `fetchTransactionsFromBank()` ב-`api/bank-sync.js` הוא placeholder)
עדיין ישאיר רישום ב-`bank_auto_sync_runs` - בלעדיה, כישלון שקורה לפני שיש תנועות
להעביר ל-`ingest_bank_transactions_batch()` לא היה משאיר עקבות בכלל.

`unified_exceptions` (048/053) קיבל ענף נוסף: הרצה שנכשלה או שזוהה בה פער, וטרם
סומנה "טופל" (`acknowledge_bank_sync_run()`, פעולה שדורשת הרשאת `bank_import.perform`
אמיתית - לא ננקית אוטומטית).

**קובץ נוסף**: `api/bank-sync.js` (Vercel Cron, `vercel.json` חדש עם `0 1 * * *` -
פעם ביום; תדירות שבועית/חודשית לכל חשבון נבדקת בפועל בתוך `is_bank_sync_due()`
ב-SQL, לא ברמת התזמון של Vercel עצמו). מסך הגדרות חדש: `/finance/bank-auto-sync`
(`BankAutoSyncScreen.tsx`) - תדירות + הפעלה/כיבוי + היסטוריית הרצות + סימון "טופל".

**ביקורת ותיקון (אותו יום)**: 5 באגים נמצאו ותוקנו ישירות בקובץ הזה לפני שהורץ בפועל
(טרם רץ נגד הפרויקט החי בעת התיקון, ולכן נערך במקום, לא ב-patch נפרד): (1) קריטי -
`ingest_bank_transactions_batch()` עשתה `raise` אחרי שכבר רשמה 'failed' ב-run - מבטל
את כל הטרנזקציה כולל הרישום עצמו (PostgREST מריץ קריאת RPC כטרנזקציה אחת) - כישלון
היה נעלם בלי עקבות. הוסר ה-`raise`. (2) משיכה חודשית ליום 29-31 הייתה מדלגת בשקט על
חודשים קצרים (פברואר וכו') - `is_bank_sync_due()` תוקנה לרוץ ביום האחרון של החודש
באותם מקרים. (3) FK חדש (`bank_auto_sync_settings.organization_bank_account_id`)
קיבל `on delete cascade` כדי לא להוסיף עוד נקודת חסימה למחיקת נתוני דמו. (4) חיזוק
הרשאות (`revoke ... from public, anon`) על `acknowledge_bank_sync_run()`, תואם לתיקון
דומה ב-071. (5) קלט "יום בחודש" בטופס הוגן מפני 0/ריק.

## שלב 24: 075 - "זכירת בחירה אחרונה" (עמותה/חשבון/קבוצה/צוות), 2026-07-29

לבקשת Chani, אחרי סבב סקירת UX מקיף ("תעבור על המערכת... איפה כדאי להקל"): כמעט בכל
מסך כספים/תפעול מכריח לבחור מחדש את אותה עמותה בכל ניווט. `user_ui_preferences`
(מפתח-ערך פר-משתמש, RLS לפי בעלות בלבד - אותו עיקרון בדיוק כמו `saved_filters` משלב
14, כולל 4 policies נפרדות ולא `for all` אחת, לצורך עקביות) + hook צד-לקוח
`useLastSelected(key, fallback)` (`src/lib/useLastSelected.ts`) - מוחלף פנימה כתחליף
ישיר ל-`useState` בבורר עמותה/חשבון/קבוצה/אחראי בעשרות מסכים. נשמר ב-DB (לא
localStorage) כדי שהבחירה תישמר גם במעבר בין מחשבים.

## שלב 24 (המשך) - איחוד מסכים וניקוי UX, 2026-07-29

באותו יום, בהמשך לאותה בקשה, בוצע סבב איחוד מסכים ופישוטים (קוד צד-לקוח בלבד, בלי
מיגרציות נוספות מעבר ל-075):

- **בנק**: תנועות בנק / התאמות בנק / משיכה אוטומטית אוחדו למסך אחד (`BankScreen.tsx`)
  עם טאבים - שלושת הנתיבים הישנים (`/finance/bank-transactions` וכו') נשארו חיים
  (בוחרים רק טאב פתיחה שונה), כדי לא לשבור קישורים/כרטיסי חריגה קיימים.
- **הגדרות מנהל**: "סוגי תנועה" ו"כללי זיהוי בנק" אוחדו ל-`BankClassificationScreen.tsx`
  (אותה הרשאה בדיוק לשניהם) בנתיב `/admin/bank-classification`.
- **משימות**: "כל המשימות"/"לוח"/"לוח שנה"/"משימות צוותים" אוחדו למסך אחד
  (`TasksAllScreen.tsx`) עם מתג תצוגה וסינון צוות משותף; "משימות חוזרות" ו"תבניות"
  קופלו לתוך הגדרות המשימות כקטעים מתקפלים (סגורים כברירת מחדל), באותו דפוס שכבר
  שימש ל"צוותים"/"קטגוריות".
- **מרכז יבוא** (`/ops/import-center`) נבנה מחדש מאפס: המנוע הגנרי הישן
  (`import_profiles`/`commit_import_batch`) לא כתב לשום טבלה עסקית אמיתית - הוחלף
  ברכזת אמיתית עם טאב לכל סוג נתונים שיש לו כבר יבוא מאומת בפועל (עמותות/סניפים/
  קבוצות, זכאות, שגיאות תלמוד, ביקורות, רשימות טלפוניות, תנועות בנק) - כל טאב עוטף
  את הלוגיקה/ה-RPC המקורי בלי כפילות, והמסכים המקוריים נשארו זמינים גם הם במקומם.
  לא נוסף יבוא לתלמידים/תרומות/חלוקות/מס"ב/כללי עמלה - אין להם יבוא אמיתי היום.
- אוטומציות מילוי-אוטומטי (מכסות, טווח תאריכי יצוא תלמוד, שדות "קודם" ברטרו, סכום
  התאמת בנק), תזכורת אופציונלית בזמן יצירת משימה, ותיקוני תצוגה קטנים נוספים (סיבת
  חסימת קידום תלמיד, טאבים במסך זכאות, חיפוש במועמדי יצוא, `type="month"` בדוחות,
  מסנן ביומן ביקורת, deep-link לחיפוש הגלובלי, שינוי תפקיד למשתמש מאושר בלי השבתה
  קודם - דרך ה-RPC הקיים `admin_set_user_status`, בלי מיגרציה חדשה).

## שלב 25: 080 - יבוא/יצוא אקסל לכל מסך שבו מוסיפים רשומות ידנית, 2026-07-29

לבקשת הלקוח, המשך לסבב הרחבת יבוא/יצוא. שני חלקים נפרדים לגמרי באופיים:

**חלק א' (מיגרציה `080_stage25_bank_classification_import.sql`)** - יבוא אקסל למסך
`BankClassificationScreen.tsx` (שני הטאבים שאוחדו אליו בשלב 24: "סוגי תנועה"/"כללי זיהוי
בנק"). על גבי מנוע היבוא הכללי (`import_profiles`/`import_batches`/`import_rows`, שלב 5) -
לא `bank_import_batches`/`rows` הייעודי לתנועות בנק עצמן (036) - זה יבוא-קטלוג/הגדרות,
אותו עיקרון בדיוק כמו `master_data` (054). שני פרופילים חדשים (`bank_transaction_types`,
`recognition_rules`) + שני RPCs חדשים (`commit_transaction_types_import_batch`,
`commit_recognition_rules_import_batch`), שניהם מוגנים ב-`transaction_classification.perform`
הקיימת - לא נדרשה הרשאה חדשה.

`commit_transaction_types_import_batch`: שני עמודות (קוד/תווית), קוד כפול (מול הטבלה או
מול שורה קודמת באותה אצווה עצמה) מסומן invalid בלי לחסום את שאר האצווה.

`commit_recognition_rules_import_batch`: כל שדות `recognition_rules` (036) כולל שני
אילוצי ה-check שלה (`text_match_type`/`text_match_value` תמיד ביחד או ריקים ביחד;
`effective_until >= effective_from`) - מאומתים במפורש בעברית *לפני* ה-INSERT (לא רק
נתפסים כ-exception גנרי), כדי שהודעת השגיאה תכוון בדיוק לאיזה עמודה לתקן. **החלטה בעלת
שיקול דעת**: עמודת "חשבון בנק" (אופציונלית) מותאמת מול `organization_bank_accounts_view`,
לא מול `organization_bank_accounts` הבסיסית - כי מספר החשבון הגולמי חסום מחוץ ל-view/
`reveal_*` RPC מוגן (060, שלב 16), כך שממילא רק הטקסט הממוסך ("שם בנק · מספר ממוסך", בדיוק
כפי שמוצג בעמודת "חשבון" בטבלה ומיוצא לאקסל) נגיש לכל מי שיכול להכין קובץ יבוא. "לא ריק
אך לא נמצא" מסומן invalid במפורש (לא null בשקט) - כדי שלא ייווצר כלל עם scope רחב מהמיועד
בלי שהמשתמש ישים לב.

צד לקוח: פאנל יבוא גנרי אחד חדש (`BankCatalogImportPanel`, בתוך `BankClassificationScreen.tsx`)
מוזן ע"י `profileKey`/`commitRpc` ומשמש את שני הטאבים - אותו דפוס תצוגה בדיוק כמו
`EligibilityImportPanel`/`AuditsScreen`, בלי בורר עמותה/חודש (שני הפרופילים גלובליים).
כפתור "ייצוא לאקסל" נוסף לשני הטאבים, מייצא בפורמט עמודות זהה למה שהיבוא מצפה לו (כולל
קוד סוג התנועה ברשימת הכללים, שנוסף ל-`select` הקיים כדי לתמוך בעגול-הלוך-חזור יבוא/יצוא).

**חלק ב' (ללא מיגרציה)** - `DistributionsScreen.tsx`: מנגנון מילוי-אוטומטי מקובץ שכבר היה
קיים (`handleInstructionFile`, פרסור ישיר דרך `parseImportFile`, לא מנוע היבוא הכללי - זו
עזרה במילוי טופס בצד לקוח, לא רשומות עצמאיות; `saveLines()` הקיימת היא שכותבת בפועל,
full-replace) היה זמין רק בשיטת `instruction_file`. הורחב לזמין בכל שיטה שמציגה טבלת
סכום-פר-תלמיד (כל שיטה חוץ מ-`percentages`, שבה הטור הוא אחוז ולא סכום ו-`saveLines`
מתעלמת מ-`amounts` לגמרי במצב הזה) - כולל `fixed_amounts`, שלא היה לה קודם שום עזרה
במילוי-אוטומטי מעבר לטבלת checkbox+input ידנית. מזהה תלמיד מהקובץ שלא נמצא בקבוצה נזרק
בעבר בשקט לגמרי (ה-`continue` בלולאה) - עכשיו נאסף ל-`fileWarnings` ומוצג כאזהרה מפורשת
עם רשימת המזהים שלא נמצאו, כדי שסה"כ נמוך מהצפוי לא יישאר תעלומה. נוסף גם כפתור "ייצוא
לאקסל" לשורות החלוקה המוצגות כרגע (מזהה תלמיד/שם/סכום), גיליון "שורות חלוקה".

## שלב 25: 076 - יבוא Excel לתלמידים (יצירה בכמות), 2026-07-29

לבקשת הלקוח: יבוא (bulk create) וייצוא לאקסל בכל מסך שבו נוצרות רשומות ידנית, החל
מתלמידים. עד כה לא היה יבוא אמיתי לתלמידים כלל - הערת הסגירה של שלב 24 ("לא נוסף
יבוא לתלמידים/תרומות/חלוקות/מס"ב/כללי עמלה - אין להם יבוא אמיתי היום") מתייחסת בדיוק
לפער הזה עבור תלמידים; המיגרציה הזו סוגרת אותו.

אותו דפוס מוכח בדיוק כמו זכאות/שגויים/ביקורות/קובץ אב (018/019/042/054): פרופיל יבוא
חדש (`students`) על גבי המנוע הגנרי הקיים (013, לא נגעו בו), ו-`commit_students_import_batch(p_batch_id)`
- security definer, מוגנת ב-`has_permission('students', 'manage')` (אותה הרשאה בדיוק
שמגנה על יצירה ידנית ב-`StudentsListScreen.tsx`), עם בדיקת סטטוס-אצווה מוקדמת (אותה
תוספת שנעשתה ב-063 לפונקציות commit אחרות) וחסימת commit כשיש שורות "דורש החלטה" פתוחות.

לכל שורה: `id_type` (מתקבל גם כערך enum באנגלית וגם כתווית עברית מהטופס הידני - ת"ז/
דרכון/אחר - וגם ריק/לא מזוהה, ברירת המחדל הרגילה israeli_id בכל מקרה), `external_id`
ו-`full_name` חובה (חסר מסמן invalid), טלפון מנורמל בדיוק כמו בצד לקוח
(`regexp_replace(phone_raw, '[^0-9]', '', 'g')`, זהה ל-`form.phone.replace(/\D/g, "")`
ב-StudentsListScreen), ותאריך לידה/כתובת/קוד לימוד אופציונליים (פרסור תאריך כושל לא
מפיל את השורה). כפילות לפי `(id_type, external_id)` מול תלמידים קיימים (008, unique)
מדולגת ונספרת בנפרד (`duplicate_count`) מ"שגוי" (`invalid_count`) - לא חוסמת את שאר
האצווה, אותו עיקרון בדיוק כמו ב-018/054. תלמיד חדש תמיד נוצר עם `status='draft'`
במפורש (גם ש-`students_enforce_status_transition`, 008, היה אוכף זאת ממילא) - התפקיד
היחיד של הפונקציה הזו הוא ליצור טיוטה, בדיוק כמו הטופס הידני; כל שאר מחזור החיים
(שיוך, חשבון בנק, קידום סטטוס) ממשיך להתבצע בכרטיס התלמיד כרגיל.

צד לקוח: `StudentsImportPanel.tsx` (קובץ חדש, אותה מסגרת בדיוק כמו `EligibilityImportPanel.tsx`
במרכז היבוא - ניתוח קובץ, לשוניות תקין/דורש החלטה/שגוי, פתרון שורות, commit, היסטוריית
יבוא - רק בלי בורר עמותה/חודש כי תלמיד לא קשור לעמותה בשלב היצירה) - מוצג כקטע מתקפל
ב-`StudentsListScreen.tsx` דרך כפתור "יבוא מאקסל" ליד "תלמיד חדש", באותו דפוס בדיוק
של הצגת/הסתרת יבוא ב-`BankTransactionsScreen.tsx`. נוסף גם כפתור "ייצוא לאקסל" באותו
מסך (`exportRowsToExcel`, שימוש שני בפונקציה אחרי `ReportsScreen.tsx` בלבד) - מייצא את
רשימת התלמידים הטעונה כרגע (עמוד נוכחי + מסננים פעילים), לא את כל הטבלה.

## שלב 25 (המשך): 082 - יבוא Excel לקודי לימוד, 2026-07-29

באותה בקשה, דומיין שני. אותו דפוס בדיוק כמו 076 לעיל: פרופיל יבוא חדש (`study_codes`)
+ `commit_study_codes_import_batch(p_batch_id)` - security definer, מוגנת ב-
`has_permission('study_codes', 'manage')` (אותה הרשאה בדיוק שמגנה על יצירה ידנית
ב-`StudyCodesScreen.tsx`, מוענקת ל-system_admin בלבד, 012), עם אותה בדיקת סטטוס-אצווה
מוקדמת וחסימת "דורש החלטה" פתוח.

לכל שורה: `code` ו-`description` חובה (חסר מסמן invalid), `category` אופציונלי. ייחוד
קוד לימוד הוא `code` בלבד, גלובלי (012, unique) - לא צירוף שדות. שורה עם קוד שכבר קיים
מדולגת ונספרת בנפרד (`duplicate_count`) מ"שגוי" (`invalid_count`), אותו עיקרון כמו 076.

מספור המיגרציה (082, לא 077) מכוון: סבב עבודה מקביל אחר בשלב 25 השתמש כבר במספרים
077-081 באותה חלונית זמן - נבחר מספר גבוה יותר כדי להימנע מהתנגשות, לא בטעות.

צד לקוח: `StudyCodesImportPanel.tsx` (קובץ חדש, אותה מסגרת בדיוק כמו `StudentsImportPanel.tsx`
לעיל) - קטע מתקפל ב-`StudyCodesScreen.tsx` דרך כפתור "יבוא מאקסל" ליד "קוד חדש", וכפתור
"ייצוא לאקסל" שמייצא את כל קודי הלימוד המוצגים כרגע במסך (`exportRowsToExcel`).

## שלב 25 (המשך): 078 - יבוא Excel לתרומות (יצירה בכמות), 2026-07-29

באותה בקשת הלקוח (יבוא/ייצוא אקסל בכל מסך שבו נוצרות רשומות ידנית), דומיין שלישי -
תרומות. מיגרציה 078 נבחרה מראש (לא 077) כדי לא להתנגש עם סבב עבודה מקביל אחר שרץ
באותו שלב 25 ותפס את 076-077 ו-080-082 לדומיינים אחרים (תלמידים/ביקורות/סיווג בנק/
מסמכים/קודי לימוד) - ר' גם ההערה תחת 082 לעיל.

אותו דפוס מוכח בדיוק כמו זכאות (018): פרופיל יבוא חדש (`donations`) על גבי המנוע
הגנרי (013), ו-`commit_donations_import_batch(p_batch_id)` - security definer, מוגנת
ב-`has_permission('donations', 'manage')` (אותה הרשאה בדיוק שמגנה על יצירה ידנית
ב-`DonationsScreen.tsx`). בשונה מזכאות: כאן שורות "דורש החלטה" (כפילות מדויקת של שורה
בתוך הקובץ, לא חוסר-ודאות עסקית) *לא* חוסמות commit - הן מעובדות יחד עם שורות "תקין",
כי תרומה כפולה-לכאורה היא עדיין תרומה לגיטימית שאין סיבה לעכב.

לכל שורה: תאריך תרומה וסכום (`> 0`, כמו ה-CHECK של הטבלה, 030) חובה - חסר/לא תקין
מסמן invalid; שם קבוצה אופציונלי (חיפוש `group_id` דרך `groups` + `branches` לפי
`organization_id` של האצווה - קבוצה לא נמצאת מסמנת invalid, לא מנחשת); אסמכתת תורם/
אסמכתה/הערות אופציונליים. העמותה נלקחת מ-`import_batches.organization_id` (לא פרמטר
נפרד ל-RPC) - אותה מוסכמה בדיוק כמו `commit_eligibility_batch`. התרומה נוצרת תמיד
בסטטוס `pending` (ברירת המחדל של העמודה; `enforce_donation_mutation_guard`, 030, חוסם
כל ערך אחר ב-INSERT ממילא) - זהה ליצירה ידנית, ה-RPC לא קובע סטטוס במפורש. v1: מטא-דאטה
בלבד, בלי קובץ מקור פר-שורה (`source_file_path` נשאר null) - העלאת קובץ בודד לכל תרומה
לא מתאימה לזרימת יבוא בכמות.

צד לקוח: `DonationsImportPanel.tsx` (קובץ חדש) - אותה מסגרת בדיוק כמו
`EligibilityImportPanel.tsx`, אך מוטמע ישירות בתוך `DonationsScreen.tsx` (לא מסך/נתיב
עצמאי במרכז היבוא) ולכן מקבל את העמותה כ-prop מהמסך המכיל, בלי בורר עמותה כפול - נפתח
דרך כפתור "יבוא מאקסל" ליד "תרומה חדשה". נוסף גם כפתור "ייצוא לאקסל"
(`exportRowsToExcel`) שמייצא את רשימת התרומות הטעונה כרגע (לפי העמותה/הסינון הנבחרים).

## שלב 25 (המשך): 079 - יבוא Excel לכללי עמלה (יצירה בכמות), 2026-07-29

באותה בקשה, דומיין רביעי. אותו דפוס בדיוק כמו 078 לעיל: פרופיל יבוא חדש
(`commission_rules`) + `commit_commission_rules_import_batch(p_batch_id, p_organization_id)`
- security definer, מוגנת ב-`has_permission('commission_rules', 'manage')` (אותה
הרשאה בדיוק שמגנה על יצירה ידנית ב-`CommissionRulesScreen.tsx`). שלא כמו 078/018 -
כאן העמותה מגיעה כפרמטר מפורש (`p_organization_id`) ולא נלקחת מהאצווה, כי הרצת הבדיקות
לכל שורה (חיפוש קבוצה, כללי ה-CHECK) דורשת את זהות העמותה. גם כאן שורות "דורש החלטה"
(כפילות מדויקת בקובץ בלבד) מעובדות יחד עם "תקין", אותו נימוק כמו 078.

לכל שורה, כל שדות ה-scope אופציונליים: שם קבוצה (חיפוש כמו ב-078, לא נמצא = invalid),
קוד לימוד (מול `study_codes.code`, לא קיים = invalid), מזהה תלמיד חיצוני (חריג לתלמיד
ספציפי, מול `students.external_id`, לא נמצא = invalid). סוג חישוב חובה מתוך רשימה
סגורה (`percentage`/`fixed`/`combined`) - לא מנחש ברירת מחדל. כל בדיקות ה-CHECK של
הטבלה (025) - `percentage` בין 0-100, `fixed_amount >= 0`, `effective_until >= effective_from`
- מבוצעות כאן מראש בעצמנו לפני ה-INSERT (לא מסתמכים על שהטבלה תדחה את השורה), כדי
ששורה שגויה בודדת תסומן invalid עם סיבה ספציפית ולא תפיל את כל האצווה. כלל עיגול
וברירת עדיפות (0) מקבלים ברירת מחדל זהה לברירת המחדל של העמודה עצמה כשריקים בקובץ.

צד לקוח: `CommissionRulesImportPanel.tsx` (קובץ חדש, אותה מסגרת בדיוק כמו
`DonationsImportPanel.tsx` לעיל - מקבל עמותה כ-prop, בלי בורר כפול) - נפתח דרך כפתור
"יבוא מאקסל" ליד "כלל חדש" ב-`CommissionRulesScreen.tsx`. נוסף גם כפתור "ייצוא לאקסל"
שמייצא את כללי העמלה הטעונים כרגע לעמותה הנבחרת, כולל שם הקבוצה/תלמיד החריג בתרגום
לטקסט קריא (לא מזהי UUID גולמיים).

## שלב 25 (המשך): 077 - יבוא Excel לאירועי ביקורת (הקונטיינר, לא רשימת החסרים), 2026-07-29

באותה בקשה (יבוא/ייצוא בכל מסך שבו נוצרות רשומות ידנית), דומיין שלישי - ושונה במפורש
מהיבוא הקיים כבר במסך הביקורות: `AuditsImportPanel.tsx`/`commit_audit_attendance_batch`
(042) מייבאים **רשימת חסרים לתוך ביקורת קיימת בודדת**. המיגרציה הזו מייבאת בכמות את
אירוע הביקורת עצמו (עמותה/סניף/תאריך) - הרשומה המנהלית הקלה שנוצרת היום ידנית שורה-
שורה ב-`AuditsScreen.tsx` (INSERT ישיר, בלי RPC), בלי שום קשר לרשימת חסרים.

פרופיל יבוא נפרד לגמרי, `audits_container` (לא `audit_attendance`, כדי שהיסטוריית שני
סוגי היבוא לא תתערבב) + `commit_audits_import_batch(p_batch_id, p_organization_id)` -
security definer, מוגנת ב-`has_permission('audits', 'manage')` (אותה הרשאה בדיוק שמגנה
על יצירה ידנית של אירוע ביקורת), עם בדיקת סטטוס-אצווה מוקדמת וחסימת "דורש החלטה" פתוח,
כמו שאר ה-commit RPCs בשלב הזה.

לכל שורה: "סניף" (לא חובה - מאותר לפי `talmud_branch_code` או `internal_name` בהיקף
העמותה; אם צוין ולא נמצא - שורה שגויה; אם ריק - `branch_id` נשאר null, "כל הסניפים",
זהה לברירת המחדל בטופס הידני), "תאריך ביקורת" (חובה, שורה שגויה אם לא ניתן לפרסור).
`audits` (042) אין לה אילוץ ייחודיות על (עמותה, סניף, תאריך) - כפילות מותרת טכנית
בסכימה, ולא הומצא כאן כלל ייחודיות חדש. במקום זאת: אם אותו שילוב עמותה/סניף/תאריך
מופיע פעמיים **באותו קובץ שמיובא עכשיו** (בדיקה מוגבלת ל-`source_batch_id` של האצווה
הנוכחית בלבד, לא מול כל היסטוריית `audits`), המופע השני מסומן `needs_decision` -
"ייתכן כפילות" - ולא נחסם, אותו עיקרון בדיוק כמו הכלל הגנרי ב-`classifyRows` (012)
לשורה שחוזרת בדיוק על שורה קודמת באותו קובץ. אירוע ביקורת חוזר לאותו סניף/תאריך
בקובץ נפרד (או ביקורת עוקבת יום אחר) אינו כפילות ואינו נחסם.

צד לקוח: `AuditsContainerImportPanel.tsx` (קובץ חדש תחת `src/areas/ops/audits/`, בכוונה
לא באותה תיקייה כמו `AuditsImportPanel.tsx` של מרכז היבוא, כדי למנוע בלבול בין שני
מושגי "יבוא" שונים על אותה ישות) - קטע מתקפל ב-`AuditsScreen.tsx` דרך כפתור נפרד
וברור-מנוסח "יבוא אירועי ביקורת מאקסל" בראש המסך, ליד "אירוע ביקורת חדש" - במפורש
**לא** ליד כפתור "יבוא רשימת חסרים" הקיים (שנשאר בתוך כרטיס הביקורת הנבחרת, בלי
שינוי), כדי ששני פעולות היבוא השונות על אותו מסך יישארו חד-משמעיות ויזואלית וטקסטואלית.
נוסף גם כפתור "ייצוא לאקסל" באותו מסך - מייצא את רשימת הביקורות הטעונה כרגע (תאריך/
סניף/סטטוס) לגיליון "ביקורות".

## שלב 25 (המשך): 081 - יבוא Excel למסמכי עמותה (מטא-דאטה בלבד), 2026-07-29

דומיין רביעי, עם מגבלה מפורשת בכוונה: `documents` (044) דורשת `file_path` או
`external_link` (check constraint) - יבוא מאקסל לא יכול לצרף קובץ פר-שורה בפועל, אז
גרסה זו (v1) תומכת רק במסמכים עם **קישור חיצוני** - `external_link` הוא שדה חובה בקובץ
המיובא, ו-`file_path` נשאר `null` תמיד עבור שורות שיובאו כך. מסמך עם קובץ מצורף אמיתי
ממשיך להיווצר כרגיל בטופס הידני הקיים (`submitDocument()`, `DocumentsScreen.tsx`).

פרופיל יבוא חדש, `documents_metadata` + `commit_documents_import_batch(p_batch_id,
p_organization_id)` - security definer, מוגנת ב-`has_permission('documents', 'manage')`
(אותה הרשאה בדיוק שמגנה על יצירה ידנית), עם אותה בדיקת סטטוס-אצווה מוקדמת וחסימת "דורש
החלטה" פתוח.

לכל שורה: "סוג מסמך" ו"כותרת" חובה (חסר מסמן invalid) - `document_type` נשאר טקסט חופשי
בטבלה עצמה (044: "לא רשימה סגורה, החלטה עסקית פתוחה"), אין enum/check constraint על
העמודה בסכימה הקיימת, ולכן ה-commit כאן לא ממציא רשימה סגורה משלו; הבדיקה היחידה היא
"לא ריק", כמו כותרת. "תאריך הנפקה"/"תאריך תפוגה" אופציונליים (פרסור כושל מסמן את השורה
כשגויה, לא מפיל את כל האצווה). "קישור חיצוני" **חובה** - חסר מסומן invalid עם הודעה
שמסבירה שיבוא קבוצתי דורש קישור כי אי אפשר לצרף קובץ דרך אקסל. "רגיש" בוליאני סובלני
(כן/true/yes/1, ברירת מחדל לא).

צד לקוח: `DocumentsImportPanel.tsx` (קובץ חדש, אותה מסגרת בדיוק כמו שאר פאנלי היבוא
הדומיין-ספציפיים בשלב הזה) - קטע מתקפל בטאב "מסמכים" של `DocumentsScreen.tsx` דרך כפתור
"יבוא מסמכים מאקסל" ליד "מסמך חדש", עם אזהרה מפורשת בראש הפאנל שהיבוא הזה מיועד למסמכים
עם קישור חיצוני בלבד ("למסמך עם קובץ יש להעלות אותו ידנית במסך הרגיל"). נוסף גם כפתור
"ייצוא לאקסל" באותו טאב - מייצא מטא-דאטה בלבד (סוג/כותרת/תאריכים/קישור/רגיש/סטטוס) של
רשימת המסמכים הטעונה כרגע לגיליון "מסמכים"; **לא** מייצא את תוכן הקובץ המצורף עצמו
בשום מקרה, גם כשקיים `file_path`.

## 083 - Patch לפרויקט החי: תמיכה רב-בנקאית במשיכה האוטומטית (מרכנתיל + פאג"י), 2026-07-29

לפי עדכון Chani: ללקוח שלה יש גם חשבונות בבנק מרכנתיל (17) ובבנק פאג"י (52), לא רק
לאומי. **074 כבר רץ חי** (Chani אישרה), אז זה patch ולא עריכה של 074 עצמו - אותו לקח
מ-008/009 (ר' למעלה בקובץ הזה).

**מחקר** (חובה לפני שממציאים "תמיכה" שלא קיימת בפועל): מרכנתיל - אישור חזק, פורטל
מפתחים ייעודי אמיתי (mercantile.co.il/openbanking), קטלוג API, תואם PSD2, Sandbox.
פאג"י - פחות ודאי: הוא מותג/חטיבה של הבנק הבינלאומי הראשון (לא רישיון בנקאי נפרד עצמאי) -
לבינלאומי יש API עסקי אמיתי משלו, אבל **לא אומת** שהוא חל גם על חשבונות פאג"י ספציפית.
Chani צריכה לברר את זה ישירות מול הבנק/הבינלאומי, בדיוק כמו השאלה הרגולטורית שנשארה
פתוחה לגבי לאומי.

**שני באגים אמיתיים נמצאו** כשניסיתי בפועל להוסיף בנק שני לתשתית שנבנתה ב-074 (התשתית
עצמה נועדה להיות כללית - `provider` הוא עמודת טקסט חופשית, בלי דומיין-ספציפיות
בקוד ה-SQL - אבל שני מקומות בכל זאת הניחו לאומי בשקט):
1. **אין UI בכלל לבחירת ספק** - `BankAutoSyncScreen.tsx` מעולם לא כלל שדה provider
   בטופס/ב-payload, אז כל הגדרה חדשה נוצרה תמיד עם ברירת המחדל של העמודה
   (`'leumi_open_banking'`) בלי אפשרות אחרת. תוקן: בורר "בנק" חדש בטופס.
2. **`ingest_bank_transactions_batch()` חיפשה `bank_import_profiles` לפי `key = 'leumi_open_banking'` בקוד קשיח**, לא לפי `v_setting.provider` בפועל - הגדרה של מרכנתיל/פאג"י הייתה מתויגת (בשקט, בלי שגיאה) תחת הפרופיל של לאומי. תוקן ל-`where key = v_setting.provider`.

מיגרציה זו: (1) 4 שורות `bank_import_profiles` חדשות (יבוא ידני + בנקאות פתוחה, לכל
אחד משני הבנקים - "בנק חדש = שורה חדשה" כמו תמיד). (2) `check constraint` חדש על
`bank_auto_sync_settings.provider` (רשימה סגורה של 3 ספקים שאושרו/נבדקו) - לפני זה
לא הייתה שום הגנה על העמודה, טעות הקלדה הייתה נכשלת בשקט רק בזמן ריצה בפועל.
(3) `list_due_bank_sync_accounts()` - **דרש DROP לפני CREATE** (לא מספיק
`create or replace`) כי הוספתי עמודת `provider` לפלט, ו-Postgres לא מרשה לפונקציה
שכבר רצה חי לשנות את טיפוס ההחזרה בלי DROP מפורש. (4)
`ingest_bank_transactions_batch()` - `create or replace` הספיק (רק הגוף השתנה, לא
הטיפוסים) - מתקן את הבאג של חיפוש הפרופיל.

צד שרת: `api/bank-sync.js` פוצל מפונקציה אחת (`fetchTransactionsFromBank`, ממתינה
ללאומי בלבד) לשלוש פונקציות ייעודיות (`fetchFromLeumi`/`fetchFromMercantile`/
`fetchFromPagi`), עם switch לפי `account.provider` - עדיין placeholder בשלושתן (אין
עדיין מפתחות/הרשאות API אמיתיים לאף בנק), אבל כל אחת עם הערת TODO ורמת-ודאות
משלה (לאומי/מרכנתיל: ודאי שה-API קיים; פאג"י: דורש בירור קודם מול הבנק).

## 084 - Patch לפרויקט החי: כתובת מפוצלת לתלמיד (רחוב/מספר בית/עיר), 2026-07-29

לפי תשובת הלקוח (דרך Chani, בתגובה למייל השאלות על מבנה הנתונים): "כתובת - שמור
מפוצל". קובץ התלמידים הרשמי של משרד הדתות מייצא בדיוק בפיצול הזה - "כתובת" (רחוב),
"מס בית", "עיר" כשלוש עמודות נפרדות, לא שדה טקסט חופשי אחד.

**076 כבר רץ חי** (Chani אישרה "הרצתי 76-82") - זה patch, לא עריכה של 076 עצמו.

- `students` מקבלת 3 עמודות חדשות: `address_street`, `address_house_number`,
  `address_city`. העמודה הישנה `address` **נשארת בכוונה** בסכימה (לא drop) - ייתכן
  שכבר הוזנו בה נתונים; היא רק מפסיקה לשמש במסכים/יבוא/ייצוא מכאן ואילך.
- `commit_students_import_batch()` (076) - `create or replace` הספיק (טיפוסי
  קלט/פלט לא השתנו) - עודכן לקרוא 3 עמודות כתובת נפרדות מ-`raw` (שם העמודה "כתובת"
  נשאר לרחוב, תואם מילה-במילה את הקובץ הרשמי) ולכתוב ל-3 העמודות החדשות.
- צד לקוח: `types.ts`, `StudentDetailsTab.tsx` (טופס עריכה + תצוגה), `StudentDetailScreen.tsx`,
  `StudentsListScreen.tsx` (עמודת טבלה + ייצוא לאקסל), `StudentsImportPanel.tsx`
  (טקסט העזרה) - כולם עודכנו מ-`address` יחיד ל-3 השדות. תצוגה מאוחדת (רחוב + מספר
  בית + עיר) בכל מקום שמציג כתובת כטקסט קריא.

**עדיין פתוח** (לא נבנה - ממתין לתשובה מלאה מהלקוח): הבחנת אברך/בחור (מאושר שמשפיעה
על סכום, אך "קוד לימוד" כבר מכסה זאת בפועל - ר' 700/720/600/300 בתשובת הלקוח), מנגנון
חישוב לפי ניקוד (ניקוד קבוע לכל קוד × "שווי ניקוד" כללי-מערכתי שמתפרסם ע"י משרד
הדתות/החינוך - עדיין לא ברור מי קובע את גובה הניקוד לכל תפקיד והאם הוא קבוע או משתנה),
מלגה, מנגנון קיזוז ברטרו, ורשימות הוספה/העזיבה לקבוצה.
