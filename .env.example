# העתק לקובץ .env.local (לא נכנס ל-git) ומלא ערכים אמיתיים מ-Supabase Settings → API.
# אלה ערכים ציבוריים המיועדים לצד לקוח (anon key) — לא ה-service_role key.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
